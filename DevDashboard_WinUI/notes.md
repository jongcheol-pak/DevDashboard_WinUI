## 최근 변경
- 2026-07-21: **테스트 화면(TestPage) 시안 정합 후속 수정 10건 — 목록 외형·필터 결함·메모 다이얼로그** (PRD FR-E1·E3·E5, plan T1~T4). 직전 재구현의 육안 확인(F-8)에서 사용자가 지적한 10건을 처리. **T1(필터 탭)**: "전체" 탭이 필터를 해제하지 못하던 결함 — 전체 탭만 `Tag=""`였고 핸들러가 `is not RadioButton { Tag: string tag }` 패턴이라, 빈 attribute가 `null`로 파싱되면 **조용히 `return`해 직전 필터("미실행")가 그대로 남는다**(체크는 되므로 밑줄·색만 바뀌어 "색상만 변한다"는 관찰과 일치). `Tag="All"` + `Tag as string`으로 바꿔 null·빈문자열·"All"을 모두 전체로 해석 — 어느 파싱 결과여도 성립. ⚠️ **가설 한계**: 빈 문자열로 파싱되고 있었다면 이 수정은 동작상 no-op이며 원인은 코드 밖이다(실동작 확인은 사용자 몫, 재현 시 런타임 재조사). 선택 탭 굵기는 `FilterTabStyle`의 `Checked`에 `Content.FontWeight=SemiBold` 추가로 해소하되, **ContentPresenter의 FontWeight가 자식에 상속**되어 개수 배지까지 굵어지므로 배지 4곳에 `FontWeight="Normal"`로 상속을 끊었다. **T2(행 재구성)**: 행 루트를 Border로 감싸 상단 1px 구분선(첫 행 선이 헤더-목록 경계가 된다), 상태 아이콘을 원형(`CornerRadius=11`) → **라운드 사각형(6)**으로 바꾸고 통과·실패는 상태색 채움+흰 글리프, 미실행은 저투명+회색 테두리로 분기(신규 헬퍼 `StatusIconBackground`/`StatusIconBorderBrush`/`StatusGlyphForeground`), 상태 pill에 `MinWidth=52`+가운데 정렬로 3종 폭 통일, 메모가 있으면 이름 아래 **인용 블록**(좌측 warning 바 + 깃발 `E7C1` + 본문, `NoteVisibility` 조건부) 표시. **hover는 `DataTemplate` 안에서 VSM `GoToState`가 동작하지 않아** `PointerEntered`/`PointerExited` 핸들러로 배경을 직접 바꿨고(색은 `UserControl.Resources`의 `RowHoverBrush`), Exited 복원값을 `null`이 아닌 `Transparent`로 둬 hit-test 영역이 좁아지지 않게 했다. **T3(스위트 헤더)**: 진행바 두께가 통과율에 따라 달라 보이던 원인은 **WinUI `ProgressBar`의 트랙과 인디케이터 높이가 서로 다른 기본 디자인** — 0%면 얇은 트랙만, 값이 있으면 두꺼운 인디케이터가 보인다. `Grid(120×4)` + 트랙/인디케이터 `Border` 2겹으로 직접 그려 두께를 확정(`IndicatorWidth(PassRate)`). 헤더 우클릭 메뉴는 **기능째 제거**(사용자 확정) — `ContextFlyout` + 핸들러 2개 + VM `RenameSuite`/`DeleteSuite`까지. `Rebuild()`의 빈 스위트 조건에서 상태 필터 종속(`&& SelectedStatus is not null`)을 빼 **표시 항목이 없는 스위트는 전체 필터에서도 카드째 숨김**. ⚠️ `TestCategory`는 작업 카테고리 이름의 **스냅샷 복사본**이라 앱 설정 변경이 전파되지 않는데 정리 UI마저 사라져, 오타·구 이름 스위트를 지울 경로가 없어졌다(Deferred `[스위트 정리 경로 부재]`). **T4(메모 다이얼로그)**: 인라인 `ContentDialog`+맨 TextBox → 신규 `TestNoteDialog`(ContentDialog 서브클래스, 제목 "메모 추가" + 대상 테스트명 부제 + 안내 placeholder + 취소/저장). placeholder는 **접미 없는 베어네임 resw라 `x:Uid`로 붙이면 빌드는 통과하되 빈 문자열**이 되므로(함정 3) 코드비하인드에서 `PlaceholderText`로 주입. 신규 키 `TestNote_Title`·`TestNote_Placeholder` ko/en 양쪽. **아이콘 재수정(같은 날 재지적)**: 렌더를 시안과 다시 대조한 결과 **미실행이 "라운드 사각형 테두리 + 내부 ○ 글리프"라 원이 이중으로 보여** 시안(속이 빈 원 하나)과 크게 달랐다. Q1 답변("세 상태 모두 라운드 사각형")보다 **시안을 우선**해 정정 — 미실행만 `CornerRadius=11`(원)·배경 투명·글리프 없음으로, 통과·실패는 라운드 사각형 채움 유지(`StatusIconCornerRadius` 추가, `StatusGlyph`의 미실행은 빈 문자열, `StatusIconBackground`의 미실행은 투명). **교훈**: 아이콘 형태처럼 렌더로만 판정되는 항목은 텍스트 선택지(preview)로 합의해도 실제 화면과 어긋날 수 있어, 시안 이미지가 있으면 그것이 최종 기준이다. **Phase F 반영**: 완료 리뷰가 ① FR-E5의 "**에러** 표시" 몫이 미착수인데 PRD Coverage가 ✅로 오표기됨(`TestItem`에 에러 필드 자체가 없음) → 부분 커버로 정정하고 대장 `[FR-E5 에러 표시 미착수]` 등재, ② **`Border.Background`는 테두리 안쪽에만 그려지므로** 채움 아이콘에 `BorderThickness=1.5`를 남기면 색 사각형이 19×19가 돼 미실행(22×22)보다 작아 보임 → `StatusIconBorderThickness`로 채움 상태는 0을 반환하게 수정. **검증**: 클린 리빌드 오류 0·경고 5건(기존 NU1903 1 + CS0612 4)만, task별 spec·quality 리뷰 지적 0(T3 quality의 고아 resw MAJOR 1건은 plan D11·대장의 계획된 이연임을 근거로 반증해 철회). **시각·실동작은 사용자 육안 확인 대기**(plan `## F-8 인계 목록`). **변경 파일**: `TestPage.xaml(.cs)`·`TestPageViewModel.cs`·`Resources/Styles.xaml`·`Views/Dialogs/TestNoteDialog.xaml(.cs)`(신규)·`Strings/{ko-KR,en-US}/Resources.resw`.
- 2026-07-21: **테스트 화면(TestPage)·테스트 등록 다이얼로그 시안 정합 재구현 — 스위트를 "작업 카테고리"로 정렬해 칸반 통과율 배지 배선 완성** (PRD `docs/prd.md` FR-E1·E3·E5 + FR-E4/FR-T8, plan T1~T5). 사용자가 "테스트 화면부터 잘못 구현돼 배지가 안 뜬다"며 시안 이미지 2장 제공(① 새 테스트 등록 다이얼로그 ② 테스트 화면). **배경**: 배지는 "작업 카테고리 이름 == 테스트 스위트 이름"일 때만 뜨는데(FR-T8), 테스트 스위트는 자유 입력이라 이름이 어긋나 사실상 죽어 있었다 → 이번에 **스위트 선택지를 작업 카테고리로 고정**해 근본 해소. **T1**: `TestPageViewModel` 생성자에 `AppSettings` 주입(호출부 `ProjectCardViewModel.CreateTestPageViewModel` 1곳), `AvailableCategories`(작업 카테고리)·`SelectedSuiteFilter` 추가, `Rebuild()`에 스위트 필터를 상태 필터와 **직교** 적용(통계 카드·탭 개수는 프로젝트 전체 기준 유지). 품질 리뷰 MAJOR로 카테고리 결합식이 3곳(TaskEditDialogViewModel·TaskPageViewModel·신규) 중복임이 드러나 `AppSettingsDialogViewModel.ResolveTaskCategories(AppSettings)`로 추출(3회 문턱), 쓰기 전용 `_settings` 필드 제거. **T2**: 등록/편집 다이얼로그 스위트를 자유 입력 ComboBox → **작업 카테고리 비편집 드롭다운**(`SelectedItem` 바인딩), 라벨 "스위트 (작업 카테고리)", 이름 필수 `*`+하단 danger 라인(TaskEditDialog 선례), 라벨 `InputLabelStyle`, 제목 "새 테스트 등록"·새 테스트 버튼 "등록"(신규 키 `TestEdit_Submit`), placeholder 시안 문구. **중요 방어**: 편집 대상의 기존 스위트가 작업 카테고리에 없으면(과거 자유명·"작업" 스위트) VM 생성자가 그 값을 옵션에 보강 — 안 하면 ComboBox 선택이 비어 필수 검증에 막히고 **기존 스위트가 소실**된다. **T3**: 헤더에 프로젝트명 배지(`TagBadgeStyle`+`AppMutedSoftBrush`)·스위트 필터 드롭다운("전체"+작업 카테고리)·"+ 테스트 등록" 버튼. **T4**: 통계 카드를 가로 3등분(`StatCardStyle` 신규, 좌 색점+라벨 / 우 22pt 큰 숫자)으로, 상태 필터를 헤더 라디오 → **개수 배지 포함 밑줄형 탭바**(`FilterTabStyle` 신규)로 이설. `FilterTabStyle`은 **함정 7**대로 선택 밑줄(CheckStates)과 호버 오버레이(CommonStates)를 별도 요소로 분리. **T5**: 테스트 항목을 카드 → **행**(좌 원형 상태 아이콘 22px + 이름 / 우 상태 pill), 스위트 그룹을 카드형 Border + 헤더(폴더 아이콘·이름·"N/M 통과"·우측 진행바 120px)로 재구성. 인라인 콤보·버튼을 없애고 **상태 pill 클릭 순환(통과→실패→미실행) + 우클릭 메뉴**(행: 수정/메모/삭제, 헤더: 이름수정/삭제)로 대체 — 발견성 보완용 pill 툴팁 추가. 신규 정적 헬퍼 `StatusSoftBrush`(0x28 알파)·`StatusText`·`FormatPassCount`·`NextStatus`(함정 5: x:Bind 함수 바인딩은 Brush 직접 반환). 마지막 소비처가 사라진 `StatusOptions`·`TestStatusOption`·`NoteVisibility`·`MethodVisibility`·`FormatPassRate`·`StatusCombo_*` 제거(잔존 참조 0, TaskPage 동명 심볼은 별개라 무영향). **변경 파일**: `TestPageViewModel.cs`·`ProjectCardViewModel.cs`·`AppSettingsDialogViewModel.cs`·`TaskPageViewModel.cs`·`TaskEditDialogViewModel.cs`·`TestEditDialog.xaml(.cs)`·`TestEditDialogViewModel.cs`·`TestPage.xaml(.cs)`·`Resources/Styles.xaml`·`Strings/{ko-KR,en-US}/Resources.resw`. resw는 ko/en 대칭 유지(함정 2), x:Uid 소비 키는 접미 형식 유지(함정 3). 도메인·스키마·직렬화 무변경. **검증**: MSBuild x64 클린 리빌드 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만), 전 task spec·quality 리뷰 통과(T1 MAJOR 1+MINOR 1 수정 후 재리뷰 OK, 나머지 지적 0). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 다이얼로그 스위트 드롭다운·이름 하단 라인 / 헤더 배지·필터 / 통계 카드·탭 밑줄(선택 탭 호버 시 밑줄 유지 = 함정 7 회귀) / 스위트 그룹·행 레이아웃·pill 클릭 순환·우클릭 메뉴 / **통합: 새 테스트를 작업 카테고리 스위트로 등록 후 칸반 그 카테고리 그룹에 통과율 배지가 뜨는지**(궁극 목표). **미결 follow-up(Deferred)**: NotificationPage 시안 대조 / 이미지의 실패색이 붉으나 PRD·코드는 호박(#E8B45A) 유지 / 기존 자유명·"작업" 스위트 테스트는 마이그레이션 안 함(go-forward) / 상태 soft 브러시의 Palette 이관 검토 / 행에서 방법·메모 미표시(데이터·편집 경로는 유지) / 스위트 조작이 우클릭이라 발견성 낮을 수 있음. 브랜치 `task/testpage-design-align` — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **작업 칸반 카테고리 그룹 통과율 배지가 안 뜨던 버그 수정 (FR-T8 배선 완성)**. 사용자가 이미지처럼 칸반 카테고리 헤더 옆 통과율 배지("✓ 통과율 67% · 4건")가 안 보인다고 신고. **조사 결과 배지 계산·렌더 코드는 이미 정상**: `TaskPageViewModel.BuildPassRateBadge`(:153-166)가 `AppSuccessSoftBrush` pill로 렌더(`TaskColumnGroupTemplate`, `TaskPage.xaml:135-146`), 데이터도 `CreateTaskPageViewModel`이 `EnsureTestsLoaded()`로 로드함(로딩 문제 아님). **근본 원인**: `BuildPassRateBadge`는 FR-T8 명세대로 "작업 카테고리 이름 == 테스트 스위트 이름"일 때만 배지를 표시하는데, "테스트 추가" 토글로 만든 링크 테스트는 `CreateLinkedTest`가 **항상 고정 "작업"(TaskLinkedTestCategory) 스위트**로 넣어 이름이 어긋남 → 사용자가 테스트 페이지에서 작업 카테고리와 같은 이름의 스위트를 손수 만들지 않는 한 배지가 영구히 미표시(이미지의 "67%·4건"은 4건으로 67%가 불가능한 조합이라 시안 placeholder — 실제 렌더된 적 없음). **수정(사용자 승인 방향: 링크 테스트를 카테고리별 스위트로)**: `CreateLinkedTest`(`TaskPageViewModel.cs:206`)가 링크 테스트를 `todo.Category` 이름의 스위트(없으면 생성, 대소문자 무시)에 넣도록 변경, 빈 카테고리(미분류)는 배지 비대상이라 기존 "작업" 스위트로 폴백. 변수 `workCategory`→`suite`, docstring·주석 갱신. 단일 파일·약 6줄. 호출부(`TaskPage.xaml.cs:193`)는 `AddTodo`로 `todo.Category` 설정 후 호출되어 안전. `TaskLinkedTestCategory` resw는 폴백에 계속 쓰여 존치. PRD FR-T6에 배선 정정 노트 추가. **go-forward만**: 기존에 "작업" 스위트로 들어간 링크 테스트는 이동하지 않으므로 과거 데이터는 배지에 반영 안 됨(마이그레이션 미실시 — 위험·범위 초과). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만), code-quality 리뷰 BLOCKER/MAJOR/MINOR 0. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 새 작업을 카테고리(예: UI·UX) 지정 + "테스트 추가" ON으로 만든 뒤, 칸반 그 카테고리 그룹 헤더에 통과율 배지가 뜨는지 / 테스트 페이지에 그 카테고리 이름 스위트가 생겼는지.
- 2026-07-21: **작업(TaskPage) 카드 — 제목·설명 최대 2줄 표시 + 말줄임(...)** (2회 수정). 사용자가 칸반 카드에서 제목이 1줄로만 보이고 긴 설명이 잘림 없이 전부 표시되던 것을, 제목 2줄·설명 2줄로 제한하고 넘치는 부분은 `...`로 줄여 보이게 요청. **1차 수정(불충분)**: `TaskPage.xaml` 카드 템플릿의 `TextBlock`에 `MaxLines="2"` + `TextTrimming="CharacterEllipsis"` 추가(칸반 제목·설명, 목록 `TaskCardTemplate` 제목). **1차만으로는 무효** — 사용자 재확인 결과 카드가 가로로만 늘어나고 여전히 1줄. **근본 원인**: 칸반 뷰 `ScrollViewer`가 `HorizontalScrollMode="Auto"`라 콘텐츠(내부 `Grid`)에 **무한 너비**를 전달 → `Width="*"` 4개 열이 뷰포트에 맞춰지지 않고 카드 텍스트 길이만큼 무한정 늘어남 → 너비 제약이 없으니 `TextWrapping`/`MaxLines`가 애초에 발동하지 않음. **2차 수정(핵심)**: 내부 `Grid`에 `Width="{Binding ViewportWidth, ElementName=KanbanScrollViewer}"`(뷰포트 너비에 묶어 유한화 → 열 균등 분배·텍스트 줄바꿈 동작) + `MinWidth="950"`(열 4×230 + 간격 3×10 = 950; 좁은 창에선 가로 스크롤 유지) 부여, `ScrollViewer`에 `x:Name="KanbanScrollViewer"` 지정. 목록 뷰는 이미 `MaxWidth="520"`으로 너비가 유한해 1차 제목 수정만으로 정상. 로직·바인딩(데이터)·resw·스키마 무변경(마크업만). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 긴 제목·설명 카드가 열 너비 안에서 각각 2줄까지 표시되고 초과분이 `...`로 잘리는지 / 창을 좁혔을 때 가로 스크롤이 정상 동작하는지.
- 2026-07-21: **작업 편집 다이얼로그 — 미분류(빈 카테고리) 항목 편집 시 콤보가 비어 보이던 문제 수정**. 사용자가 미분류 작업을 클릭해 편집하면 카테고리 콤보가 빈 상태로 열리던 것을 첫 항목이 표시되게 요청. **원인**: 직전 작업(T2, D6)에서 편집 모드의 빈 카테고리를 `null`(미선택)로 열어 그대로 저장 시 빈 값을 보존하도록 의도했으나, 사용자는 콤보가 비어 보이는 것을 원치 않음. **수정**: `TaskEditDialogViewModel.cs:80`의 편집 로드 분기를 `string.IsNullOrEmpty(existing.Category) ? null : existing.Category` → `? CategoryOptions[0] : existing.Category`로 변경(첫 카테고리 "UI·UX" 기본 표시), 주석도 갱신. `BuildResult`의 `?? string.Empty` 방어는 그대로 유지(무영향). **의도된 동작 변경**: D6 결정을 뒤집는 것으로, 미분류 항목을 편집 후 저장하면 카테고리가 "UI·UX"로 확정되는 것이 요청된 정상 동작이다. 마크업·resw·스키마 무변경(1줄+주석). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **새 작업 다이얼로그 — "테스트 추가" 카드 토글이 세로로 내려가던 문제 수정(시안 정합)**. 사용자가 현재 화면(토글이 "테스트 추가" 텍스트 **아래 왼쪽**에 세로 배치)과 시안(토글이 헤더 **오른쪽**에 인라인)을 비교하며 시안대로 수정 요청. **원인**: "테스트 추가"는 `toolkit:SettingsCard`(CommunityToolkit.WinUI.Controls.SettingsControls 8.2)로 구현돼 있는데, `SettingsCard`는 카드 폭이 `SettingsCardWrapThreshold`(기본 476px)보다 좁으면 콘텐츠(토글)를 헤더 아래로 내리는 적응형 세로 wrap이 동작한다. 다이얼로그 콘텐츠 `MinWidth="360"`이라 카드 실폭이 476px 미만 → wrap 발동 → 토글이 아래로 내려감(직전 07-21 항목에서 "이미 시안과 동일, 무변경"으로 판단했으나 실제 앱은 wrap 상태였음 — 정정). **수정**: `TaskEditDialog.xaml`의 해당 `SettingsCard.Resources`에 `SettingsCardWrapThreshold`·`SettingsCardWrapNoIconThreshold`를 각 **0**으로 덮어써 세로 wrap을 끔(카드가 항상 NormalState=토글 우측 인라인 유지). CommunityToolkit에서 wrap을 끄는 표준 방식(임계값 리소스 오버라이드). 토글 색(ON=살몬/OFF=회색)은 `IsOn`(`Vm.AddTest`) 상태에 따른 accent 색으로 기존과 동일 — 시안2의 빨간 토글은 ON 상태일 뿐이며 별도 변경 아님. 마크업만 변경(로직·바인딩·resw 무변경, 5줄 추가). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 다이얼로그를 열었을 때 "테스트 추가" 토글이 헤더 오른쪽에 인라인으로 정렬되는지. 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **새 작업 다이얼로그 — 등록 버튼 필수 입력 기반 활성/비활성화** (시안 피드백 후속). 사용자가 "테스트 추가 항목을 이미지와 동일하게 + 등록 버튼은 필수 항목이 모두 입력되면 활성, 미입력이면 비활성" 요청. **① "테스트 추가" 항목은 이미 시안과 동일** — `toolkit:SettingsCard`(Header "테스트 추가" + Description "이 작업을 테스트 목록에도 추가합니다." + 우측 `ToggleSwitch`)로 직전 작업에서 구현·커밋된 상태라 무변경(검토만). **② 등록/저장 버튼 활성화**: `ContentDialog`의 primary 버튼을 필수 입력(제목) 충족 시에만 활성화. `TaskEditDialogViewModel`에 계산 프로퍼티 `CanSubmit => !string.IsNullOrWhiteSpace(Title)` 추가, `Title`에 `[NotifyPropertyChangedFor(nameof(CanSubmit))]` 부여(제목 입력 시 실시간 갱신). `TaskEditDialog.xaml` `ContentDialog` 루트에 `IsPrimaryButtonEnabled="{x:Bind Vm.CanSubmit, Mode=OneWay}"` 바인딩(`ContentDialog`는 `FrameworkElement`라 기존 헤더 x:Bind처럼 정상 동작). 새 작업은 제목이 비어 열리므로 등록 버튼이 **초기 비활성**, 제목 입력 시 활성. 편집 모드는 제목이 미리 채워져 활성 상태로 열림. 기존 `OnSave`의 `args.Cancel` 빈 제목 차단은 방어선으로 유지. 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 새 작업 열었을 때 등록 버튼 회색(비활성) → 제목 입력 시 활성 전환. 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **새 작업 다이얼로그 후속 수정 3건 + 대시보드 복귀 네비게이션 버그** (PRD `docs/prd.md` FR-T5 후속 정정 + FR-C3 회귀 수정). 사용자가 시안 대조 피드백으로 4개 요구 제시. ① **T1 필수 입력 표시 형태 변경**: 제목 입력칸의 필수 강조를 "빈 값일 때만 4면 danger 테두리"(`TitleBorderBrush` x:Bind 함수 바인딩)에서 **"값 유무와 무관하게 상시 하단 2px 빨간 라인"**으로 변경. `TextBox`를 `Grid`로 감싸고 하단 정렬 `Border`(Height 2, `AppDangerBrush`, CornerRadius 0,0,4,4, **`IsHitTestVisible="False"`** — 라인이 입력 클릭을 가로채지 않게)를 겹침. `TitleBorderBrush` 정적 헬퍼·정적 브러시 2개·고아 `using`(Microsoft.UI, Microsoft.UI.Xaml.Media)·`xmlns:local` 제거, `OnSave` 주석 갱신(라인이 상시 표시라 "비어 있음" 신호로 못 쓰므로 `args.Cancel` 차단이 유일 방어선), 차단 로직은 유지. **부수효과**: 라인이 TextBox 형제라 이전 작업 D3-a(hover 시 빨간 테두리가 회색으로 전환되던 문제)가 해소됨. ② **T2 카테고리 "미분류" 선택지 제거**: `TaskEditDialogViewModel`의 `CategoryOptions`에서 선두 "미분류"(`_noneCategoryLabel`) 제거하고 필드 자체 삭제. 새 작업은 `CategoryOptions[0]`("UI·UX") 자동 선택. **`SelectedCategoryOption`을 `string?`로 전환** — 편집 시 빈 카테고리는 `null`(미선택)로 열고, `BuildResult`에서 `?? string.Empty`로 흡수. **계획 리뷰(B1)가 구현 전에 잡은 것**: `Todos.Category`는 `TEXT NOT NULL`(`DatabaseContext.cs:254`)이고 쓰기는 `BuildResult` 1곳뿐인데, ComboBox가 목록에 없는 값을 거부하며 null을 TwoWay로 되써넣을 수 있어(미확인 프레임워크 동작) null이 새면 저장 실패 → `?? string.Empty` 방어로 ComboBox 동작에 무관하게 성립. ③ **미분류 표시 유지**: 카테고리가 빈 기존 작업의 칸반·목록 그룹 헤더 "미분류" 표시는 이미 `TaskPageViewModel:140,173`(빈 값 → 표시 시점에만 "미분류")이 담당 → **무변경**, `TaskCategory_None` resw 키도 존치(지우면 `LocalizationService.Get`이 raw 키를 반환해 표시가 깨짐). ④ **T3 대시보드 복귀 네비게이션 버그**: 작업 페이지 → 대시보드 복귀 → 다시 작업 클릭 시 무반응. **근본 원인**: `DashboardView`는 `MainWindow._dashboardView`로 **재사용되는 단일 인스턴스**인데, `OnUnloaded`가 `Unloaded -=`·`DataContextChanged -=`로 자기 핸들러를 떼고 카드 구독을 해제. 복귀 시 `DataContext`(MainViewModel)가 안 바뀌어 `DataContextChanged`가 재발동 안 하고 그 핸들러마저 떨어져 있어 카드 이벤트 6종(작업/테스트/Git상태/작업기록/명령슬롯 2종)이 **영구히 죽음**. 수정: `Loaded` 핸들러 추가(복귀 시 재구독 지점 — `ReferenceEquals(_subscribedVm, vm)` 가드로 중복 구독 방지, Loaded/Unloaded 처리 순서 역전에도 안전), `OnUnloaded`는 핸들러 자기해제 2줄 제거하고 `UnsubscribeAll`만 호출. 구독 로직을 **카드 수준(`SubscribeCards`/`UnsubscribeCards`) + VM 수준(`SubscribeAll`/`UnsubscribeAll`) 4개 메서드로 추출**(계획 리뷰 M2: 한 쌍으로 합치면 `OnDisplayCardsChanged`의 Reset 분기가 자기 `CollectionChanged`를 해제·재등록하는 다른 동작이 됨 → Reset은 카드 전용만 사용). 빌드 MSBuild x64 클린 리빌드 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). 전 task spec·quality 리뷰 통과(T3 quality MINOR=필드 주석 stale 수정 후 재확인 OK), F-7 plan-completion-reviewer BLOCKER/MAJOR/MINOR 0, Phase G Must 100%. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: T1 하단 라인의 렌더 일치(두께·모서리·포커스 시 TextBox 자체 강조선과의 중첩) / T3 네비게이션 왕복 동작(대시보드↔작업 3회 이상 재진입·카드 버튼 6종·복귀 후 프로젝트 추가 시 새 카드 버튼·왕복 후 다이얼로그 중복 없음). 미결 follow-up(Deferred): 카테고리 필터에서 "미분류"만 골라보기 부재 / 삭제된 카테고리를 가진 작업 편집 시 값 소실 / 카테고리 없는 작업을 새로 만들 수단 부재(D5 자동 선택의 귀결). 브랜치 `task/taskedit-dialog-design-align`(이전 TaskEditDialog 시안 작업과 같은 브랜치에 이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-20: **새 작업(TaskEditDialog) 다이얼로그 시안 정합 재구성** (PRD `docs/prd.md` FR-T5·FR-T6). 사용자가 새 작업 화면 시안 이미지를 제공하며 "이미지처럼 수정" 요청 — 직전 칸반 작업과 같은 원인(Phase 2 구현 당시 목업 부재로 PRD 텍스트만으로 구현)이며 **기능 결함이 아니라 시각 구조 불일치**. ① **T1 resw 문구 정비**(ko/en): placeholder 4키 값을 시안 문구로 교체(제목="무엇을 해야 하나요?"·설명="작업 내용 설명 (선택)"·시작/종료일="연도-월-일"), 카드용 `TaskEdit_AddTestCard.Header`/`.Description`(x:Uid 소비라 **속성 접미 필수**)와 버튼용 `TaskEdit_Submit`(코드 `Get()` 소비라 베어네임) 추가. ② **T2 VM 확장**: `TaskEditDialogViewModel`에 `HeaderTitle`/`StatusLabel`/`PrimaryButtonLabel` 추가 + 생성자에 `TodoStatus status = TodoStatus.Waiting` 선택적 파라미터. 상태는 `existing?.Status ?? status`로 **편집이면 기존 항목 상태가 우선** — 덕분에 편집 호출부(`EditTodoAsync`)는 무변경이고 `ColumnAdd_Click`만 열 상태를 넘긴다. 새 작업 시작일 기본값을 오늘로(`new DateTimeOffset(DateTime.Today)` — plan이 제시한 `DateTimeOffset.Now.Date`는 `DateTime`을 반환해 `DateTimeOffset?`에 대입 불가). ③ **T3 XAML 재구성**: 헤더를 `<ContentDialog.Title>` 커스텀 콘텐츠(제목 + 상태 pill[`TagBadgeStyle`+`AppMutedSoftBrush`])로 바꾸고 **코드비하인드의 `Title` 대입을 제거**(남기면 XAML Title을 런타임에 덮어써 pill이 사라진다). 라벨 6개를 `BodyStrongTextBlockStyle`→**기존에 정의만 되고 소비처가 0이던 `InputLabelStyle`**(12px·저강도)로 교체 + 제목 라벨에 빨간 `*`. 제목이 비면 danger 테두리를 상시 표시(`TitleBorderBrush` 정적 헬퍼, ARGB 리터럴 — x:Bind 함수 바인딩은 ThemeResource를 못 받음), 하단 오류 문구(`ErrorText`)는 제거하되 **`args.Cancel` 저장 차단은 유지**. "테스트 추가"를 `toolkit:SettingsCard`(제목+부제+우측 토글, `AppSettingsDialog` 선례)로 카드화. 버튼 문구 새 작업="등록"/편집="저장"(공용 `Dialog_Save`는 다이얼로그 8종이 쓰므로 **값을 건드리지 않고** 전용 키 신설). 참조가 끊긴 `TaskEdit_AddTest.Header`·`TaskEdit_TitleRequired` 2키를 ko/en에서 제거. **신규 스타일 0 — 전부 기존 자산 재사용.** 빌드 MSBuild x64 클린 리빌드 오류 0·신규 경고 0. **계획 리뷰가 구현 전에 잡은 것 2건**: (a) 내가 "accent==danger 동색이라 상태 전환에도 시각이 안 어긋난다"고 논증했으나 `TextControlBorderBrushPointerOver`만 **회색(#35353C)으로 오버라이드**돼 있어 hover 시 빨간 테두리가 사라짐 → 빨간 `*`가 상시 보조하므로 수용으로 확정(D3-a), (b) 고아 키 제거를 T1에 뒀는데 그 시점엔 아직 소비 중이고 `LocalizationService.Get`은 **미발견 키를 키 문자열 그대로 반환**해 화면에 raw 키가 노출됨 → T3로 이관. **구현 리뷰 지적**: T2에서 T3 몫(`Title` 제거·버튼 문구 교체)을 선반영해 그 커밋 단독으로는 **헤더 제목이 빈 상태로 렌더되는 회귀**가 남 → 원복 후 T3에서 배선. **빌드가 잡은 함정**: `{x:Bind TitleBorderBrush(...)}`는 static을 인스턴스로 접근해 CS0176 — `TaskPage.xaml:82` 선례대로 `xmlns:local` + 타입 한정 필요. **시각 렌더 일치는 빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: pill 인접 배치·라벨 크기/간격·danger 테두리·`SettingsCard` 배경 대비·토글 살몬색·전체 여백. 미결 follow-up(Deferred): 시작일 기본값 도입으로 **"시작일 없는 작업"을 만들 수단이 사라짐**(`CalendarDatePicker`에 날짜 지우기 UI 없음)·다른 다이얼로그의 라벨 스타일 불일치(`InputLabelStyle` 소비처가 0이었다는 건 다른 화면도 굵은 라벨을 쓴다는 뜻)·오류 문구 제거로 접근성(스크린 리더) 피드백 약화·선택적 `status` 파라미터가 3번째 호출부에서 누락될 위험.
- 2026-07-20: **작업(TaskPage) 칸반 화면 시안 정합 재구성** (PRD `docs/prd.md` FR-T2·T3·T4·T5·T6·T8). 사용자가 디자인 시안 이미지를 제공하며 "구현된 작업 화면이 이미지하고 전혀 다르다"고 지적 — 원인은 Phase 2 구현 당시 목업 HTML이 레포에 없어 PRD 텍스트만으로 만든 것(당시 plan Investigation Log에 기록됨)이며 **기능 결함이 아니라 시각 구조 불일치**. 시안↔현행 10개 항목 대조표를 만들어 재구성. ① **T1 시각 자산**: `Palette.xaml` Default 딕셔너리에 `AppInfo*`(파랑 #5B93D8)·`AppWarning*`(주황 #D9954A) + 배지 배경용 저채도(알파 0x28) 브러시 4종 추가(**Light 딕셔너리 신설 금지** — Phase 0의 다크 단일 정책 유지). `Styles.xaml`에 `KanbanColumnStyle`(열 패널)·`DashedAddButtonStyle`(WinUI `Border`는 점선 미지원 → 템플릿 안 `Rectangle StrokeDashArray="3,3"`, Stroke 필수)·`SegmentedToggleStyle` 추가. resw ko/en 6키. **PRD FR-T8·FR-E4 문구 갱신**(카드 배지 → 카테고리 그룹 통과율 배지 — 요구 자체가 바뀌므로 코드보다 PRD를 먼저 수정, 사용자 승인). ② **T2 칸반 전용 카드**: `TaskKanbanCardTemplate` 신규(제목+우선순위 pill / 설명 / `MM-dd – MM-dd` 날짜 범위 1줄). 카드 클릭=편집, 우클릭 `ContextFlyout`=편집·상태 변경(4종)·삭제. **기존 `TaskCardTemplate`은 칸반·목록 공용이라 본문 무변경 존치**(주석 1줄만 갱신) — 목록 뷰엔 `AllowDrop`이 없어 상태 콤보가 유일한 상태 변경 수단이므로 존치가 기능상 필수. `EditTask_Click`/`DeleteTask_Click` 본문을 `EditTodoAsync`/`DeleteTodoAsync`로 추출해 목록 버튼·칸반 클릭·우클릭이 공용. ③ **T3 열별 그룹핑·통과율**: `TaskColumnGroup` record 신규, 칸반 4개 컬렉션의 요소 타입을 `TodoItem`→`TaskColumnGroup`으로 교체(사용처는 `TaskPage.xaml` 1곳뿐 — 전수 grep 확인). `FillColumn`→`BuildColumnGroups`, `BuildPassRateBadge`(같은 이름 `TestCategory` 기준·`OrdinalIgnoreCase`·불일치/0건/미분류는 미표시, `TestPageViewModel.Rebuild()` 계산식 재사용)·`CountItems` 신규. 열 4개를 패널 Border + 상태 dot + 그룹 템플릿 + 점선 추가 버튼으로 교체, **드롭 대상을 안쪽 StackPanel → 열 Border로 이동**(빈 열에도 드롭 가능). ④ **T4 헤더 정리**: `+새 작업` 헤더 버튼 제거(열 하단 점선 버튼이 대체), 칸반/목록을 아이콘+라벨 세그먼트 토글로, 카테고리 콤보를 앞으로. 미사용 심볼 정리(`TaskPageViewModel`의 `TotalCount`·`ShowKanban`/`ShowList` RelayCommand·고아 `CommunityToolkit.Mvvm.Input` using — `TestPageViewModel.TotalCount`는 별개 클래스이며 사용 중이라 보존). resw `TaskAdd_Button` 제거 + `TaskView_Kanban/List`를 베어네임으로 통일(아이콘+라벨 구성상 `x:Uid` 불가 → 코드 정적 참조 소비). 빌드 MSBuild x64 클린 리빌드 오류 0·신규 경고 0. **적대적 계획 리뷰가 구현 전에 잡아낸 함정 3건이 이번 작업의 핵심**: (a) `TaskCardTemplate`이 목록 뷰와 공용이라 그 자리에서 고치면 목록이 강제로 함께 바뀜, (b) `Vm.CreateLinkedTest`의 **유일한 호출부가 삭제 대상인 헤더 `AddTask_Click`**이라 그냥 지웠으면 FR-T6("테스트 목록에도 추가")가 조용히 죽음 → `ColumnAdd_Click`으로 이관, (c) `WaitingCount = WaitingItems.Count`를 그대로 두면 컬렉션이 그룹 목록이 되어 **컴파일 오류 없이 "작업 수" 대신 "카테고리 수"**가 표시됨 → 그룹 합계로 재계산. 구현 리뷰 지적: SegmentedToggle의 `GoToState` 덮어쓰기 함정(같은 파일 `SettingsNavItemStyle` 주석이 경고하던 패턴 — 요소 분리로 수정), fire-and-forget `_ =`가 예외를 무음 처리(→`async void`+`await`로 통일), 우선순위 브러시 주석 부정확, 고아 using. **담당자(FR-T7)는 사용자 결정으로 제외 유지, 목록 뷰는 시안 미확보로 무변경. 시각 렌더 일치는 빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY).** 미결 follow-up(Deferred): `BuildPassRateBadge` 조회 테이블화(현 규모에선 과한 최적화)·완료 열에 새 작업 즉시 생성 시 작업기록 팝업 미발생(`AddTodo`는 `WorkLogRequested` 미발생 — 기존 설계가 새 경로에 노출된 것)·목록 뷰/TestPage/NotificationPage 시안 대조·AGENTS.md 부재.
- 2026-07-19: **리디자인 Phase 5 — 알림: 마감 감지 + 전체 페이지 + 헤더 드롭다운 + 읽음 영속화** (PRD `docs/prd.md`, FR-N1~N4). 리디자인 로드맵 마지막 Phase. ① **마감 감지 도메인**(FR-N1, T1): 신규 `Domain/Enums/DeadlineStatus`(Imminent/DueToday/Overdue)·`Domain/ValueObjects/Notification`(record: ProjectId/ProjectName/TodoId/TodoText/EndDate/Status)·`Domain/Services/NotificationService`(프로젝트 최초 도메인 서비스 — 정적 순수 `Detect(projects, today)`: `EndDate!=null && Status!=Completed` 필터 → 남은일수 3/2/1=임박·0=오늘·음수=경과·4+ 제외 → EndDate 오름차순, `BuildKey`=`작업Id:마감상태`). `GlobalUsings`에 Domain.Services 등록(소비처 생기는 T3에서). ② **읽음 영속화**(FR-N4, T2): `AppSettings.ReadNotificationIds`(List<string>) 추가 — AppJsonContext 통짜 직렬화 자동 커버·`ApplyTo` 제자리 mutate로 설정 다이얼로그 경로에서도 보존(둘 다 코드 대조 검증, 무수정). ③ **집계·읽음 처리·이동**(T3): `MainViewModel`에 `GetAllProjectItemsWithTodos`(WithHistories 미러·GetTodos 재사용)·`RebuildNotifications`(집계+`UnreadNotificationCount` 갱신+**읽음 키 프루닝**=현재 알림 키와 교집합, 재환기로 누적된 stale 키 정리)·`MarkNotificationRead`/`MarkAllNotificationsRead`(SaveSettings)·`GetCurrentNotifications`/`GetReadKeys`·`CreateTaskPageViewModelForProject`(id로 카드 조회)·`HasUnreadNotifications`(배지 표시, T5 편입)·InitializeAsync 말미 초기 계산. 신규 `NotificationPageViewModel`(프로젝트별 그룹 `NotificationProjectGroup`·항목 `NotificationItemViewModel`(IsRead)·MarkRead/MarkAllRead/OpenTask 커맨드, 읽음 소유는 MainViewModel 단일·콜백 위임 — TaskPageViewModel 콜백 선례). ④ **알림 전체 페이지**(FR-N2, T4): 신규 `NotificationPage`(UserControl, TaskPage/TestPage 관례) — 정적 헬퍼(마감상태 색 Brush[경과 코랄#f0716a·오늘 앰버#e8b45a·임박 블루#5aa3e8, PRD §3]·라벨·FormatDue·UnreadDotVisibility·ReadOpacity), 프로젝트별 그룹·배지 dot·안읽음 강조·"모두 읽음"·빈 상태, 항목 클릭 Border Tapped→OpenTaskCommand·읽음 버튼 Click→MarkReadCommand(TaskPage Tag 패턴). csproj Page 자동 include라 등록 편집 불필요. ⑤ **헤더 벨 + 드롭다운 + 배지**(FR-N3, T5): MainWindow 헤더에 벨 버튼(Segoe MDL2 E7ED)+안읽음 개수 배지(`{Binding HasUnreadNotifications, Converter=BoolToVisibility}`+`{Binding UnreadNotificationCount}` — Window XAML x:Bind+Converter CS1503 회피)+Button.Flyout 드롭다운(요약 상위5 + "모든 알림 보기"). 핸들러 `NotificationFlyout_Opening`(열 때 RebuildNotifications 재계산)·`ViewAllNotifications_Click`(→NotificationPage 전환, 콜백 주입)·`NavigateToProjectTasks`(→해당 프로젝트 TaskPage, null 가드). ⑥ 다국어 resw(ko/en) 11키(Notification_Title·Back·MarkAll·Empty·DuePrefix·Deadline_Imminent/DueToday/Overdue·MarkRead_Tooltip·Header_Tooltip·ViewAll). 빌드 MSBuild x64 오류 0·신규 경고 0. **읽음 키=작업Id+마감상태(재환기 — 임박→오늘→경과 악화 시 안읽음 재등장)·클릭 이동=프로젝트 작업 페이지 열기(특정 카드 포커스 없음)·배지=안읽음 개수·단위 테스트 미추가(테스트 프로젝트 부재)는 사용자 결정. 시각 렌더(벨/배지/드롭다운/알림 페이지/마감상태 배지)는 목업 부재로 사용자 확인 필요.** 리뷰: 전 task spec/quality OK(T5 spec MINOR=Files 목록 누락→plan 정정, T5 quality SUGGEST=배지 상한→Deferred). **미결 follow-up(Deferred)**: 알림 요약 상위 N·배지 "9+" 상한(시각 확인 후 조정)·BOM/no-BOM 인코딩 통일(레거시 .cs/.xaml/.resw는 BOM 보존, 신규는 no-BOM)·README/스크린샷 리디자인 5개 영역 통합 갱신·import로 AppSettings 교체 시 읽음 상태 리셋 허용(Out of Scope)·구 resw 고아/`TestDateGroup`/`HistoryEntryDialogViewModel` 정리(Phase 2/3/4 이월).
- 2026-07-19: **리디자인 Phase 4 — 작업 기록: 유형(Kind) + 페이지네이션 + 공통 베이스 VM 정리** (PRD `docs/prd.md`, FR-H1~H4·D-5). ① **유형(Kind) 필드**(FR-H1): `HistoryEntry`에 `string Kind`(빈=미분류) POCO 프로퍼티 추가. ② **영속화 마이그레이션**(FR-H1/NFR-3): `DatabaseContext` — `AllowedIdentifiers`에 `Kind`, `MigrateSchema`에 `AddColumnIfNotExists(conn,"Histories","Kind","TEXT NOT NULL DEFAULT ''")`, `CREATE TABLE Histories`에 컬럼 추가. `SqliteProjectRepository` — `InsertHistories` INSERT 컬럼/`@kind` 파라미터 + **루프 내 값 갱신**(Phase 3 T2 @method 누락 교훈 반영, silent data loss 방지), `GetHistories`·`ReadHistoriesForProject`에 `HasColumn` 가드 read(구버전 DB 빈값). ③ **공통 베이스 VM 추출**(FR-H4/D-5): 신규 추상 `HistoryDialogViewModelBase`(ObservableObject)로 중복 로직 이전 — 검색·날짜그룹(`RebuildGroups`)·CRUD(`AddEntry`/`UpdateEntry`/`DeleteEntry`/`RefreshAfterEdit`)·마크다운 내보내기·유형(`AvailableKinds`, `BuildKinds`=DefaultHistoryKinds+settings.HistoryKinds) + **페이지네이션**(생성자 `int pageSize` 주입·`CurrentPage`·`TotalPages`·`PrevPage`/`NextPage` 커맨드·`PageInfoText`·`CanPrev/NextPage`, `RebuildGroups`가 필터→OrderBy→Skip/Take→GroupBy(날짜), 범위 클램프). `HistoryDialogViewModel`(단일)·`ProjectHistoryDialogViewModel`(다중)이 상속, 저장/프로젝트 선택만 각자 담당(추상 훅 `OnEntriesModified`·`OnEntryAdded`·`ExportProjectName`). 두 생성자에 pageSize 추가 → 호출부 4곳(`ProjectCardViewModel`·`TaskPage.xaml.cs`·`ProjectHistoryDialog.xaml.cs`·`MainWindow.xaml.cs`)이 각자 `settings.PageSize`(파생은 AppSettings 분해) 전달. `HistoryEntryViewModel`에 `Kind`·`KindVisibility` 노출. ④ **유형 선택 UI**(FR-H1/H3): 두 다이얼로그 인라인 새 기록 폼(AddPanel)에 유형 ComboBox(`AddKindCombo`, x:Uid), 코드비하인드에 `InitKindCombo`("미분류"+AvailableKinds)·`KindFromCombo`(미분류→빈) 대칭 헬퍼, 중첩 편집 다이얼로그에 유형 ComboBox → `base.UpdateEntry`에 kind 파라미터 추가·전파. ⑤ **유형 배지 + 페이지 컨트롤 restyle**(FR-H2/H3): 항목 DataTemplate에 유형 배지(Ellipse `TagColorConverter` dot + Kind 텍스트, `KindVisibility` 숨김 — 신규 컨버터 없이 작업 카드 배지 패턴 재사용), 하단 페이지 컨트롤(이전/다음 버튼 + `PageInfoText`, `HasEntries`로 숨김, 아이콘 버튼 접근성 `ToolTipService.ToolTip`). 검색·날짜그룹·펼침·내보내기·인라인폼은 기존 유지. ⑥ 다국어 resw(ko/en) 신규 키(`AddKindCombo.Header`·`HistoryKind_None`·`HistoryPage_PrevPage`·`HistoryPage_NextPage`). 빌드 MSBuild x64 오류 0. **작업 기록은 다이얼로그 유지(페이지 전환 아님 — PRD §1). 시각 렌더(유형 배지 색·정렬·페이지 컨트롤 배치)는 목업 부재로 사용자 확인 필요.** 리뷰: T3 quality MAJOR(`ProjectHistoryDialogViewModel.cs` BOM 손실→PowerShell로 복원)·T5 quality MAJOR(페이지 버튼 접근성 ToolTip 누락→PrevPageTooltip/NextPageTooltip 추가) 수정 후 전 task 통과. **미결 follow-up(Deferred)**: `HistoryEntryDialogViewModel` 소스 미참조 고아 정리(실제 편집 UI는 인라인/중첩 다이얼로그)·`InitKindCombo`/`KindFromCombo` 2곳 대칭 중복(3회째 사용 시 공용 추출)·README/스크린샷 갱신(시각 확인 후)·Todo*/Test* 구 resw 고아·`TestDateGroup` 정리(Phase 2/3 이월).
- 2026-07-19: **리디자인 Phase 3 — 테스트 전체 페이지 + 상태 모델 전환(통과/실패/미실행)** (PRD `docs/prd.md`, FR-E1~E5·D-1/D-2/D-3). ① **테스트 상태 모델 전환**(FR-E2/D-1): `TestItem` 상태 상수를 `Testing/Fix/Done` → `Pass/Fail/Untested`로 교체(비파괴 — 새 상수 추가 후 참조부 이전, 구 상수는 T6에서 구 VM/Dialog와 함께 삭제), 기본값 `Untested`, `IsCompleted => Status==Pass`, 신규 `Method`(테스트 방법) 필드. DB 값 마이그레이션 `MigrateTestStatusToNewModel`(CASE UPDATE Done→Pass·Fix→Fail·Testing→Untested, 구 값만 매칭돼 멱등)·`Method` 컬럼(`AddColumnIfNotExists`+화이트리스트)·`CREATE TABLE` DEFAULT 'Untested'·`ReadActiveTestProjectIds` `!= 'Pass'`·Repository Insert/Read Method(`HasColumn` 가드)·구버전 fallback(`isCompleted ? Pass : Untested`). ② **`TestPageViewModel`**(FR-E1/E5): 스위트(카테고리) 그룹·상태별 통계(Pass/Fail/Untested/Total)·스위트별 통과율(`TestSuiteGroup` record, 필터 무관 전체 기준)·상태 필터(`SelectedStatus`)·CRUD(스위트 추가/삭제/이름수정, 테스트 추가[`AddTestToSuite` 스위트명 매칭/생성]/편집/삭제/상태변경/메모)·FIFO 저장 체인(`QueueSave`, Phase 2 TaskPageViewModel 패턴)·`ProjectCardViewModel.CreateTestPageViewModel` 팩토리 + `RefreshTestCardState`(HasActiveTest). ③ **`TestEditDialog`**(FR-E3): 이름/스위트(ComboBox IsEditable, 기존 선택+신규 입력)/방법, `OnSave` 이름·스위트 검증, `ResultTest`+`ResultSuiteName`. 새 테스트 전용 스위트 선택(편집은 이동 미지원). ④ **`TestPage`**(FR-E1/E5): 헤더(← 대시보드·제목·상태 탭[통과/실패/미실행/전체]·새 테스트)·상태 통계 카드 3종(색 dot)·스위트 그룹(통과율 `ProgressBar`)·항목 카드(상태 아이콘/색 PRD §3: 통과 #5aa3e8 ✓·실패 #e8b45a ✕·미실행 #8a8890 ○·방법·메모·상태콤보·수정/삭제/메모). 메모 편집·삭제 확인·스위트 이름수정은 인라인 소형 ContentDialog(add/edit 경로 분리). 정적 색 브러시/글리프 헬퍼(x:Bind 함수는 Converter 미적용 → Brush/Visibility 직접 반환). ⑤ **`MapTestBadge`**(FR-E4): 작업 카드 연결 테스트 배지를 새 상수(Pass→통과/Fail→실패/Untested→미실행)로 갱신, `CreateLinkedTest` 생성 테스트 기본 미실행 승계. ⑥ **정리**(T6): 카드 "테스트" 버튼→`ShowPage(TestPage)`(다이얼로그 대체, Phase 2 페이지 네비 재사용), 구 `TestListDialog`/`TestListDialogViewModel` 3파일 삭제·csproj Page 항목 제거·`TestChanged`/`OnTestChanged` 이벤트 체인 제거(증분 저장이 승계)·`TestItem` 구 상수 제거. ⑦ 다국어 resw(ko/en) 신규 문자열(TestEdit_*·TestPage_*·TestStatus_* 등 17키). 빌드 MSBuild x64 오류 0·신규 경고 0(구 TestListDialog WMC1506 경고도 소멸). **시각 렌더(통계 카드·상태 탭·스위트 통과율·상태 아이콘 레이아웃)·영속 왕복[상태 변경→재열기]·구 데이터 마이그레이션은 사용자 확인 필요**(리디자인 목업 부재 → PRD §3 추출값 기준). 리뷰: T2 quality MAJOR(InsertTestItems `@method` 루프 바인딩 누락→silent data loss) 수정, T4 quality MAJOR(주석 코드와 반대) 수정, T5 spec BLOCKER(plan T5 acceptance 문서 정정으로 해소) 후 전 task 통과. 교차 집계(D-2)·테스트→작업 역방향 링크(FR-E4 확장)는 Deferred. 미결 follow-up: Todo*/Test* 구 resw 고아 정리(대량)·`TestDateGroup` 고아 정리·미사용 심볼(ShowKanban 등).

## 미해결 이슈
- (없음)

## 주의사항
- System.Drawing.Common NuGet 패키지 추가됨 (아이콘 추출용)
- LauncherItems 테이블이 SQLite에 추가됨 (기존 DB 자동 마이그레이션)
- MainWindow 생성자에 LauncherRepository 파라미터 추가됨
- 런처 아이템은 Button 대신 Grid 사용 (CanDrag 호환성 — Button은 포인터 캡처로 드래그 불가)
- 런처 드래그&드롭: DashboardView와 동일한 DropPlaceholder 패턴 사용 (DisplayItems: ObservableCollection&lt;object&gt;)

## 아카이브 인덱스
- 2026-03.md: 동기 DB 호출 백그라운드 전환, Import 개선, 앱 안정성(전역 예외 처리)
- 2026-04.md: 런처 중복 클릭 방지·프리징 개선, 태그 마키 최적화, README·LICENSE 신규 작성
- 2026-05.md: 커맨드 스크립트 PowerShell 따옴표 손실 수정(EncodedCommand 전환)
- 2026-06.md: 테스트 목록 기능 최초 구현
- 2026-07.md: 리디자인 Phase 0~2(다크 팔레트·설정·칸반 페이지), To-Do/테스트 상태 모델 ComboBox 전환, 테스트 카테고리 구조
