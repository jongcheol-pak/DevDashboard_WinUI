## 최근 변경
- 2026-07-23: **상단 우측 아이콘 버튼 사각 테두리 제거(카드 버튼 스타일과 통일)** (사용자 요청). 헤더 우측 5개 버튼(정렬 `SortButton`·전체 기록 `AllHistoryButton`·내보내기/가져오기 `ExportImportButton`·알림 `NotificationButton`·설정 `AppSettingsButton`, 모두 `MainWindow.xaml`)이 기본 WinUI Button 스타일이라 사각 배경·테두리가 보였다. 카드 아이콘 버튼(`CardIconButtonStyle`, 투명·무테두리·hover 시 subtle 배경)처럼 만들어 달라는 요청 → `Resources/Styles.xaml`에 공용 `TopBarIconButtonStyle`(TargetType Button, 36×36) 신규 추가: `Background=Transparent`·`BorderThickness=0`·`CornerRadius=8`, 커스텀 `ControlTemplate`(Border 루트)에서 CommonStates VSM으로 PointerOver→`SubtleFillColorSecondaryBrush`, Pressed→`SubtleFillColorTertiaryBrush`, Disabled→Opacity 0.3 (카드 버튼과 동일한 hover/pressed/disabled 동작). `Foreground=TextFillColorPrimaryBrush`로 기존 글리프 색 유지. 5개 버튼에서 인라인 `Width/Height/Padding`을 제거하고 `Style="{StaticResource TopBarIconButtonStyle}"` 적용(반복이라 공용 스타일화가 정당). 추가로 탭 행의 **새로고침 버튼 `RefreshButton`** 도 동일 스타일 적용 — 단 이 버튼은 원래 32×32라 로컬 `Width/Height="32"`로 스타일의 36 setter를 오버라이드해 크기 유지(로컬 값이 스타일 setter보다 우선), `Padding="0"`만 제거. 카드 로컬 `CardIconButtonStyle`은 30×30이라 그대로 두고, 상단용은 36×36 별도 스타일로 분리(치수 다름). 기능·바인딩·이벤트 무변경. **검증**: MSBuild x64 Debug 빌드 오류 0(기존 `CS0618` 1건만). **빌드로 검증 불가 — 사용자 확인 필요**: 상단 6개 버튼(정렬·기록·내보내기·알림·설정 + 새로고침)이 평상시 사각 테두리 없이 아이콘만 보이고 hover 시에만 옅은 배경이 뜨는지.
- 2026-07-23: **버그 수정 — 관리자 권한 실행 시 UAC 취소하면 예외/오류 대화상자** (`ProjectCardViewModel.TryStartProcess`). 관리자 권한(`Verb="runas"`)으로 `Process.Start` 호출 시 UAC 프롬프트를 사용자가 취소하면 `Win32Exception`(`NativeErrorCode == 1223`, ERROR_CANCELLED, "사용자가 작업을 취소했습니다")가 발생한다. 기존 코드는 취소를 `OperationCanceledException`으로만 처리해서, 실제 취소 예외는 그 catch를 통과해 일반 `catch (Exception)`으로 떨어져 **"실행 실패" 오류 대화상자**가 떴다(디버거에선 first-chance로 break). 사용자가 의도적으로 취소한 건데 오류로 처리되는 잘못된 동작. **수정**: `catch (System.ComponentModel.Win32Exception ex) when (ex.NativeErrorCode == 1223)` 절을 추가해 UAC 취소를 정상 동작으로 무시(기존 `OperationCanceledException` 취소 처리 의도를 완성). 관리자 실행 성공/일반 실행/실제 실행 실패(파일 없음 등) 경로는 무변경. **검증**: MSBuild x64 Debug 빌드 오류 0(기존 `CS0618` 1건만). **동작 검증 완료(사용자 확인)**: 관리자 권한 프로젝트 실행 후 UAC 취소 시 오류 대화상자 없이 정상 무시됨. (디버거 실행 시 뜨는 first-chance 예외 팝업은 catch 여부와 무관한 VS 알림 — F5로 통과.)
- 2026-07-23: **실행 버튼 — 관리자 권한 아이콘 위치 이동 + 세로 정렬(글리프 EB59→E7EF)** (사용자 요청, 시각 마무리). 프로젝트 카드 실행 버튼(`DashboardView.xaml` Grid.Column=0) 내부 `StackPanel`에서 관리자 권한 아이콘(`RunAsAdmin` 바인딩)이 도구 이름(`DevToolName`) **앞**에 있어 "▶ ● VS 2026" 순서였다. 사용자가 ①이름 **뒤**로 이동 ②세로 중앙 정렬 ③크기 ④이름과 간격 조정을 요청 → 아이콘 블록을 `TextBlock` 뒤로 옮김("▶ VS 2026 ●"). **세로 정렬 근본 원인**: `FontIcon`의 `VerticalAlignment="Center"`는 글리프를 감싼 폰트 line-height 박스만 중앙에 두는데, 아이콘 글리프는 그 박스 안에서 세로 위치가 글리프마다 다르다 — 기존 관리자 배지 `EB59`는 박스 안에서 **아래로 치우쳐** 그려져 낮게 보였다(재생 글리프 `E768`은 박스 중앙이라 멀쩡). **해결 경위**: FontSize 확대·`TranslateTransform` 렌더 보정(매직넘버)·벡터 `PathIcon`(방패)+`Viewbox`(매직넘버 없이 정렬 보장)를 차례로 시도한 뒤, **최종적으로 박스 중앙에 잘 앉는 Admin 글리프 `E7EF`(Windows UAC 관리자 상징)로 교체**하니 순수 `VerticalAlignment="Center"`만으로 세로 중앙이 맞았다. 최종: `<FontIcon Glyph="&#xE7EF;" FontSize="13" Foreground="White" VerticalAlignment="Center" Margin="-2,0,0,0" Visibility="{Binding RunAsAdmin, ...}"/>`(`Margin` 왼쪽 -2로 `Spacing="7"` 약간 상쇄해 이름과 간격 좁힘). 바인딩·VM·resw 무변경, 신규 심볼 0. **교훈**: 아이콘 폰트 글리프의 세로 정렬이 어긋나면 크기/보정보다 **박스 중앙에 잘 앉는 다른 글리프로 교체**가 가장 간단·안정적. **검증**: MSBuild x64 Debug 빌드 오류 0(기존 `CS0618` 1건만). **동작 검증 완료(사용자 확인)**: 실행 버튼에서 관리자 아이콘이 이름 뒤·세로 중앙에 적절한 크기로 표시됨.
- 2026-07-23: **대시보드 카드 — 플레이스홀더 점선박스 통일 + 카드 hover 이동 애니메이션 + 편집 시 핀 유지** (PRD FR-D1 세부 보강·버그 수정, plan T1~T3). 사용자가 갱신한 시안(`docs/design/DevDashboard Redesign.dc.html`)과 렌더 이미지를 근거로 3가지 수정. **T1(플레이스홀더 통일)**: 갱신 시안에서 "＋ 설명 추가"(needDesc)·"＋ 태그 추가"(needTags)가 이전(테두리 없는 텍스트 링크 / 작은 좌측 pill)과 달리 noMeta와 같은 **카드 폭 전체 점선 박스**로 통일됨. `DashboardView.xaml`의 ③needDesc·⑤needTags를 공용 `DashedAddButtonStyle`로 교체(MinHeight 36·24, noMeta는 52 유지). **부수 효과로 hover가 통일됨** — 기존엔 ③이 기본 Button(hover 회색 사각 배경), ⑤가 DashedAddButtonStyle(hover 액센트 테두리)이라 "마우스 오버 색상이 다른" 문제였는데, 셋이 같은 스타일이 되며 hover 강조가 앱 액센트 산호(`AppAccentBrush`)로 일치(사용자 Q1: 시안 보라 #8b7cf7 대신 앱 액센트 유지 — 팔레트 다크 단일 정책). resw·VM·바인딩 무변경, 신규 심볼 0. **T2(카드 hover 이동 애니메이션)**: 시안 카드는 hover 시 `transform:translateY(-2px)`로 살짝 떠오른다(`:352`). 카드 `Border`에 `TranslateTransform` RenderTransform을 두고, 코드비하인드에 private static 헬퍼 `AnimateCardTranslate(Border, double toY)` 추가 — `DoubleAnimation`으로 Y를 0↔-2로 0.15s 애니메이션. `Card_PointerEntered/Exited`가 기존 BorderBrush 교체 + 이동 애니메이션을 함께 호출. **TranslateTransform은 렌더 트랜스폼이라 종속 애니메이션(`EnableDependentAnimation=true`) 필수** — 없으면 런타임 예외(quality 리뷰가 정확성 확인). 카드가 DataTemplate 안 Border라 VSM PointerOver가 자동 공급되지 않아 기존 포인터 이벤트 방식을 그대로 확장(NFR-2: 애니메이션은 View 코드비하인드, VM 무관). **T3(편집 저장 시 핀 유지)**: 신규 프로젝트 추가 시 핀은 **이미 off**였으나(ToProjectItem이 IsPinned 미설정 → false, DB DEFAULT 0), **핀 걸린 프로젝트를 편집 저장하면 핀이 풀리는 버그** 발견(사용자 Q2로 함께 수정 확정). `MainViewModel.AddOrUpdateProjectAsync`의 편집 분기가 CommandScripts만 보존하고 `Update(item)`을 호출하는데, 다이얼로그 결과 `item`의 IsPinned/PinOrder가 false/0이라 기존 핀을 덮어썼다. 기존 CommandScripts 보존 패턴을 확장해 `existing.ToModel()`의 IsPinned/PinOrder도 보존(2줄 추가). 신규 추가 분기 무변경. **검증**: F-1 클린 리빌드(`-t:Rebuild`) 오류 0·신규 경고 0(baseline `CS0618` 1건만), F-3 회귀 grep 전부 기대값, 전 task spec 리뷰 OK·quality 리뷰(T2·T3 MINOR는 후술 Deferred), F-7·Phase G 통과. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 설명만/태그만 카드의 플레이스홀더가 동일한 폭 전체 점선 박스로 보이고 hover 강조가 3종 모두 산호로 통일되는지 / 카드에 마우스 올리면 위로 살짝(≈2px) 떠올랐다 벗어나면 복귀하는지 / 핀 걸린 프로젝트 편집 저장 후 핀 유지·신규는 핀 off인지. **Deferred(quality 리뷰)**: [SUGGEST] 카드 hover Storyboard 재사용(빠른 hover 잔상 — 실사용 영향 작음), [SUGGEST] PinOrder 메모리 동기화 간극(재정렬 직후 편집 저장 시 순서 되돌림 가능 — 사전 존재 아키텍처 간극, T3 범위 밖). **변경 파일**: `Presentation/Views/DashboardView.xaml(.cs)`·`Presentation/ViewModels/MainViewModel.cs`. 브랜치 `task/dashboard-card-redesign` — master 병합 미실행(사용자 승인 대기).
- 2026-07-23: **대시보드 카드 — 빈 설명·태그 시안 플레이스홀더 + 헤더/액션 버튼 사각 테두리 제거** (PRD FR-D1 세부 보강, plan T1~T3). 사용자가 갱신한 시안(`docs/design/DevDashboard Redesign.dc.html:366~409`)을 근거로, 카드에 설명·태그가 없을 때 빈 공간 대신 "추가 유도" 플레이스홀더를 3분기 표시하고, 헤더·액션 아이콘 버튼의 사각 테두리를 시안대로 없앴다. **직전 항목(설명·태그 자리 Height 32/24 고정 예약)을 이번 시안 방식으로 대체** — 플레이스홀더가 항상 자리를 채우고 하단(액션·슬롯)은 `*` 여백이 바닥으로 민다. **T1(VM 파생값)**: `ProjectCardViewModel`에 `HasDescription`/`HasTags` + 상호 배타 `ShowMetaPlaceholder`(noMeta=둘 다 없음)/`ShowDescPlaceholder`(needDesc=태그만 있음)/`ShowTagsPlaceholder`(needTags=설명만 있음) 5개 get-only bool 추가. 세 조건이 `!D&!T`/`!D&T`/`D&!T`라 동시 true 불가, 표시 블록(D·T)과 합쳐 4조합을 빈틈·중복 없이 분할. VM은 bool만 계산해 `Microsoft.UI.Xaml` Brush 미참조(NFR-2 유지). 편집 후 갱신은 `MainViewModel:477`이 카드 VM을 **재생성**하므로 알림 배선 불요(plan-reviewer 확인). **T2(카드 템플릿+resw)**: 본문 상단을 5블록 조건부로 재구성 — ① 둘 다 없음 → 점선 박스 "＋ 설명·태그 추가"(기존 `DashedAddButtonStyle` 재사용, hover 액센트가 시안 보라 대신 앱 액센트로 이미 일치), ② 설명 → 2줄 말줄임(현행 유지), ③ 설명 없고 태그 있음 → 테두리 없는 텍스트 링크 "＋ 설명 추가", ④ 태그 → `MarqueeTagsControl`(현행 유지), ⑤ 태그 없고 설명 있음 → 점선 pill "＋ 태그 추가". 세 플레이스홀더는 클릭 시 기존 `EditCommand`(프로젝트 설정 다이얼로그) — 설명·태그 입력 수단이 그것뿐이라 신규 커맨드 0. 문구는 resw `CardPlaceholder_DescAndTags`/`_Desc`/`_Tags` ko/en 대칭(`.Text` 접미, DataTemplate 안 `TextBlock x:Uid`가 `AddNewProjectText` 선례로 동작 확인). `＋`는 전각(`&#xFF0B;`) 텍스트. **신규 스타일 0**(점선은 기존 스타일 재사용, needDesc는 투명 버튼 인라인). **T3(버튼 테두리)**: 공용 `CardIconButtonStyle`의 `BorderThickness` 기본값 1→0으로 바꿔 핀·삭제·할일·테스트·더보기 5버튼 테두리를 한 번에 제거(시안 `border:none`), 슬롯 버튼 4곳은 시안이 `1px solid`라 로컬 `BorderThickness="1"` 복원. 스타일 소비처는 `DashboardView.xaml` 9곳(카드 내부)뿐이라 다른 화면 영향 0, `Styles.xaml`·`DashedAddButtonStyle` 무변경. `PointerOver` VSM의 `BorderBrush`는 두께 0이면 비가시라 그대로 둠(동작 보존). **검증**: F-1 클린 리빌드(`-t:Rebuild`) 오류 0·신규 경고 0(baseline `CS0618` 1건만), F-3 회귀 grep 전부 기대값, 전 task spec·quality 리뷰 통과, F-7 `plan-completion-reviewer` BLOCKER/MAJOR 0(MINOR 2는 육안 항목으로 Deferred), Phase G Must 100%. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 설명만/태그만/둘 다 없음/둘 다 있음 4조합 카드가 시안대로 렌더되는지 / 플레이스홀더 클릭 시 프로젝트 설정 다이얼로그가 열리는지 / 핀·삭제·할일·테스트·더보기 버튼의 사각 테두리가 사라졌는지(hover 배경 강조는 유지) / 슬롯 버튼 테두리는 유지되는지 / 액션·슬롯이 카드마다 같은 하단 위치인지. **Deferred(F-7 지적)**: 플레이스홀더 모서리 반경 8 vs 시안 9/6, needDesc 링크의 기본 Button hover 배경 — 육안에서 거슬리면 개별 처리. **변경 파일**: `Presentation/ViewModels/ProjectCardViewModel.cs`·`Presentation/Views/DashboardView.xaml`·`Strings/{ko-KR,en-US}/Resources.resw`. 브랜치 `task/dashboard-card-redesign` — master 병합 미실행(사용자 승인 대기).
- 2026-07-23: **대시보드 카드 — 설명·태그가 없는 카드에서 하단 컨트롤(실행 버튼·슬롯)이 위로 당겨지던 문제 수정(위치 통일)**. 사용자가 "설명·배지 정보가 없으면 아래 컨트롤이 위로 표시된다, 모든 카드에서 위치를 고정하라"고 지적(예: 설명·태그가 없는 "쉼냥" 카드). **원인**: `DashboardView.xaml`의 `ProjectCardTemplate` 본문이 `StackPanel Spacing="12"`라 위에서부터 쌓이는데, 설명은 값이 없으면 `Collapsed`(`StringNotEmptyToVisibility`)·태그는 0개면 `MarqueeTagsControl` 높이가 0이 되어, 그만큼 하단 액션/슬롯 블록이 위로 당겨졌다. **수정**: 본문 `StackPanel` → **`Grid`(Auto 상단 / `*` 여백 / Auto 하단)**로 교체. 설명·태그를 상단 고정 영역(`Grid.Row=0` StackPanel)으로 묶고 **설명 `Height="32"`(2줄)·태그 `Height="24"`로 자리를 항상 예약**(내용 유무와 무관), 하단(액션+슬롯)은 `Grid.Row=2`로 두어 `*` 여백이 밀어낸다. 셀 높이가 이미 `UniformGridLayout MinItemHeight="256"`으로 균일하므로 하단 블록이 모든 카드에서 같은 위치에 고정된다. 설명의 `Visibility` 바인딩은 공간을 항상 유지하려 제거(빈 값이어도 2줄 높이). 드래그 6속성·헤더 밴드·컨버터 바인딩 등 나머지는 무변경. **검증**: MSBuild x64 Debug 빌드 오류 0·신규 경고 0(기존 baseline `CS0618`(Icon) 1건만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 설명·태그가 없는 카드와 있는 카드에서 실행 버튼·슬롯 줄의 세로 위치가 동일한지 / 설명 1줄·2줄 카드 모두 하단이 어긋나지 않는지. **변경 파일**: `Presentation/Views/DashboardView.xaml`.
- 2026-07-22: **메인 카드 목록(대시보드) 화면 — 시안 원본대로 전면 재구성** (PRD FR-D1~D4 신설, plan T1~T8). 사용자가 "디자인 화면을 확인해서 메인 카드목록 화면 수정" 요청 — 시안 원본(`docs/design/DevDashboard Redesign.dc.html`)의 대시보드 분기(`:332~404`)·카드 메뉴(`:450~487`)·카드 데이터(`:1055~1064`)·파생 스타일(`:1279~1296`)·색 팔레트(`:1450`)를 직접 읽어 시각 명세 26항목을 표로 추출한 뒤 진행했다. **⚠️ PRD 충돌 선처리**: `docs/prd.md` §7 Out of Scope가 "대시보드 카드 자체의 대규모 기능 변경(카드 액션 재배치 등)"을 **영구 제외**로 규정하고 있어, PRD가 자기 규약(`:5` "요구 변경은 PRD → plan → 코드 순서로만")에 따라 **코드보다 먼저** 정정했다 — §7에서 해당 줄 삭제, §4에 `대시보드 카드(Phase 6)` 절 신설(FR-D1~D4), §6 로드맵에 Phase 6 행, §8에 결정 `D-9` 기록. **T1(도메인·DB)**: `ProjectItem.HeaderColor`(#RRGGBB, 빈 문자열이면 이름 해시 자동) 추가 + `DatabaseContext`의 **`AllowedIdentifiers` 화이트리스트 등록** + CREATE TABLE 컬럼 + `AddColumnIfNotExists`. 화이트리스트를 빠뜨리면 `AddColumnIfNotExists`가 `ArgumentException`을 던져 **빌드는 통과하고 앱 기동이 죽는다**(계획 리뷰가 BLOCKER로 사전 검출). 읽기는 `ReadProjects` 한 곳에 `HasColumn` 가드만 추가했다 — `ReadAllFromDb`가 이를 재사용하므로 가져오기까지 자동 커버되며, 계획 초안이 "읽기 2경로"라 적었던 것은 오독이었다(리뷰가 정정). **T2(설정 다이얼로그)**: 시안 `:674`의 "카드 헤더 색상" 팔레트를 추가 — 맨 앞 "자동"(빈 문자열) + 시안 `:1450`의 10색. `HeaderColorItem`은 기존 `TagBadgeItem`(태그 선택 뱃지)과 같은 패턴. 처음엔 항목이 `Brush`를 직접 들게 했다가 **이 레포가 "VM에 Brush를 두지 않는다"를 명시 관례로 두고 있어**(`TaskPageViewModel.cs:312` 주석) quality 리뷰 MAJOR를 받고 `HexToBrushConverter`로 이관했다(T3 몫을 앞당겨 구현, plan에 기록). 선택 테두리는 하드코딩 대신 `{ThemeResource TextFillColorPrimaryBrush}` + 기존 `BoolToVisibility`. **T3(파생값)**: `TagColorConverter`의 FNV-1a+HSL 해시를 `ColorFromName`으로 public 노출해 **새 해시를 만들지 않고** 도구 배지와 색 규칙을 공유. `EffectiveHeaderColor`(지정색 우선 → 없으면 이름 해시)·`Initial`·`HasIcon` + `HexToHeaderGradientConverter`(시안 `linear-gradient(135deg, .55 → .14)` = StartPoint(0,0)/EndPoint(1,1) + 알파 0x8C/0x24). **T4(카드 상부)**: 62px 색상 헤더 밴드(그라데이션) + 38px 아바타(아이콘 있으면 아이콘, 없으면 이니셜) + 이름 14.5px + 핀/삭제 28px, 본문은 설명 **1줄 말줄임**(기존 2줄) + 태그. 드래그 6속성을 루트 `Border`에 승계했다(누락 시 핀 카드 재정렬이 죽는다). hover 테두리를 구현하면서 **`Application.Current.Resources["CardStrokeColorDefaultBrush"]` 인덱서 접근이 BLOCKER**로 잡혔다 — 그 키는 `Palette.xaml`의 `ThemeDictionaries` 안에 있어 일반 인덱서로는 조회되지 않아 **마우스를 뗄 때마다 예외**가 났을 코드다. `TestPage`·`TaskPage`가 로컬 flat 브러시 별칭을 두는 이유가 정확히 이것이었고, 같은 패턴으로 고친 뒤 색은 `Palette.xaml`에 `CardStrokeColorHover` 토큰으로 이관했다(원시 hex는 팔레트에만 두는 관례). **T5(액션 축약)**: 카드 버튼 8개 → **4개**(도구 실행 버튼 + 작업 + 테스트 + `…`). 폴더·터미널·Git 상태·작업 기록·설정·삭제는 `…` 메뉴 7항목으로 이동. **기능 손실 0을 커맨드 12종 전수 대조로 확인**했고, 특히 `OpenTerminalWithPowerShellCommand`는 삭제된 터미널 버튼의 우클릭이 유일한 진입점이었어서 `MenuFlyoutSubItem`(기본/PowerShell)으로 옮기지 않았다면 UI에서 사라졌을 것이다(계획 리뷰 BLOCKER). 삭제 항목은 위험색(quality 리뷰 지적 — plan에 "위험색"으로 설계돼 있었으나 구현에서 누락). **T6(슬롯)**: 설정된 슬롯만 표시 + 끝에 점선 `＋`(공용 `DashedAddButtonStyle` 재사용, 정의 무수정 — `TaskPage` 4곳이 공유). `CanAddCommandSlot`을 `!IsCmd3Configured`로 뒀다가 **`AddCommandSlot`의 판정 기준과 달라** quality MAJOR — "슬롯이 앞으로 당겨져 있다"는 가정은 UI 흐름에만 성립하고 DB에서 읽은 데이터엔 보장되지 않아, 마지막 슬롯만 채워진 데이터에서 빈 슬롯이 있는데도 추가가 막힌다. 양쪽 다 컬렉션을 직접 보도록 통일했다. **T7(그리드)**: `UniformGridLayout`에 `MinItemWidth=310`·간격 14·`ItemsStretch=Fill`, 3개 템플릿의 고정폭(330)을 `MaxWidth=360`으로. **드롭 위치 판정의 하드코딩 `165`(옛 카드 폭의 절반)를 실제 폭 기준으로 교체** — 폭이 가변인데 상수로 두면 핀 카드가 엉뚱한 자리에 떨어진다(계획 리뷰가 파일 누락까지 함께 지적). **T8(그룹 탭 배지)**: `ProjectGroup`이 설정 JSON으로 직렬화되는 **불변 record**라 개수를 담을 수 없어 `GroupTabViewModel` wrapper를 도입하고 탭 `ItemsSource`를 교체(코드비하인드의 `DataContext: ProjectGroup` 패턴 2곳도 함께). 이 과정에서 **앱을 켤 때마다 그룹 탭이 사라지는 회귀**가 quality BLOCKER로 잡혔다 — `Groups.CollectionChanged` 구독이 초기 로드 *뒤*라 `RebuildGroupTabs`가 한 번도 안 불렸고, 이전 코드는 `Groups`를 직접 바인딩해 드러나지 않던 문제다. 선택 표시도 값 기반 `IsSelected` 바인딩으로 바꿔 **이름 변경으로 탭이 재생성돼도 선택이 풀리지 않게** 했다(quality MAJOR). 개수는 검색어를 반영하며(시안 `:1290`), 검색 조건은 `MatchesSearch` 한 곳으로 통일해 목록과 배지가 어긋날 수 없다. 배지 색 구분(선택 액센트/비선택 회색)은 신규 컨버터 없이 기존 `BoolToVisibility` 쌍으로 처리했고, `Styles.xaml`에 만들었던 전용 스타일은 **plan Design ④("탭 전용 스타일을 만들지 않는다") 위반**으로 spec BLOCKER를 받아 인라인으로 되돌렸다(`Styles.xaml` diff 0). **태그는 현행 `MarqueeTagsControl` 유지**(사용자 미선택 — 시안 pill로의 교체는 Deferred). **검증**: 클린 리빌드(`-t:Rebuild`) 오류 0·신규 경고 0(자동 생성 `CS0618` 1건만), 회귀 grep 12종 전부 기대값, 전 task spec·quality 리뷰 통과. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: plan `## F-8 인계 목록` 참조(앱 기동·색 저장/불러오기·`…` 메뉴 각 항목·슬롯 `＋` 흐름·그리드 가변폭·드래그 재정렬·그룹 탭 배지). **변경 파일**: `ProjectItem.cs`·`DatabaseContext.cs`·`SqliteProjectRepository.cs`·`ProjectSettingsDialogViewModel.cs`·`HeaderColorItem.cs`(신규)·`ProjectSettingsDialog.xaml(.cs)`·`DevToolConverters.cs`·`Converters.xaml`·`ProjectCardViewModel.cs`·`DashboardView.xaml(.cs)`·`Palette.xaml`·`MainViewModel.cs`·`GroupTabViewModel.cs`(신규)·`MainWindow.xaml(.cs)`·`Strings/{ko-KR,en-US}/Resources.resw`·`docs/prd.md`. 브랜치 `task/dashboard-card-redesign` — master 병합 미실행(사용자 승인 대기).
- 2026-07-22: **상단 "새 프로젝트" 버튼 제거 + 카드 목록의 "새 프로젝트 추가" 카드를 항상 첫 번째로**. 사용자 요청 — 프로젝트 추가 진입점을 헤더 버튼과 카드 두 곳에서 **카드 하나로 일원화**하고, 그 카드가 목록 끝이 아니라 맨 앞에 오게 했다. **① 헤더 버튼 제거**: `MainWindow.xaml`의 AccentButton(`x:Uid="NewProjectText"`)과 소비처가 사라진 `AddProjectButton_Click` 핸들러를 함께 삭제 — 같은 동작의 `OnAddProjectRequested`(카드 → VM 이벤트 경로)가 남아 있어 기능 손실은 없다. **② 카드 순서**: `MainViewModel.ApplyFilterAndSort`의 `DisplayCards.ResetWith(sorted.Cast<object>().Append(_addCardPlaceholder))`를 `Prepend(...)`로 바꿔 플레이스홀더를 선두에 놓고, `DisplayCards` XML 주석도 순서에 맞게 갱신. **드래그&드롭 영향 없음** — `DashboardView.xaml.cs`의 드롭 플레이스홀더는 `DisplayCards.IndexOf(target)`로 위치를 잡는 상대 인덱스 방식이라 앞에 항목이 하나 늘어도 계산이 어긋나지 않는다. **미사용 잔존**: resw 키 `NewProjectText.Text`(ko/en)는 소비처가 없어졌으나 리소스 삭제는 요청 범위 밖이라 그대로 뒀다. **검증**: MSBuild x64 Debug 빌드 오류 0·신규 경고 0(기존 `XamlTypeInfo.g.cs` CS0618 1건만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 헤더에서 버튼이 사라지고 우측 정렬이 어색하지 않은지 / 추가 카드가 목록 맨 앞에 뜨고 클릭 시 등록 다이얼로그가 열리는지 / 고정 카드 드래그 재정렬이 정상인지. **변경 파일**: `MainWindow.xaml(.cs)`·`Presentation/ViewModels/MainViewModel.cs`.
- 2026-07-22: **테스트 항목 행 — 이름이 행 위쪽에 붙던 문제(세로 중앙 정렬)**. 사용자가 "제목만 있는 경우 위쪽에 표시된다, 세로 중앙으로" 지적. **원인**: 직전 작업(연결 배지)에서 행 Grid를 3단(이름/배지/메모)으로 만들면서, 배지·메모가 없는 항목은 Row 1·2가 비고 **`Grid.RowSpan="3"`인 상태 아이콘·상태 pill이 만드는 남는 높이가 빈 아래 행으로 몰려** 이름만 위로 붙었다(전체 창 스크린샷에서 pill이 이름보다 살짝 아래에 찍힌 것이 근거). 처음엔 `MaxLines="2"`가 한 줄 텍스트에도 2줄 높이를 예약하는 것으로 의심해 임시 제거·빌드했으나 사용자 실행 확인 결과 **변화 없어 가설 기각**, 원래대로 복원했다. **수정**: 이름·배지·메모를 행마다 `Grid.Row`로 쪼개는 대신 **하나의 `StackPanel`로 묶고 그 묶음에 `VerticalAlignment="Center"`**를 줬다 — 행 높이가 무엇 때문에 커지든 항상 세로 중앙이 된다. 이에 따라 `Grid.RowDefinitions`(3개)와 상태 아이콘·pill의 `Grid.RowSpan="3"`을 제거(RowSpan이 없어도 단일 행이라 세로 중앙 유지). 배지 내부 `Grid`(폭을 내용에 맞추는 Auto 2열)는 그대로 — 세로 StackPanel은 자식에게 가로 폭을 제한해 주므로 긴 제목의 말줄임 동작에 영향이 없다. **검증**: 빌드 오류 0·신규 경고 0(기존 `XamlTypeInfo.g.cs` CS0618만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 제목만 있는 행에서 이름·상태 아이콘·pill이 같은 높이로 보이는지 / 배지·메모가 있는 행의 세로 배치가 깨지지 않았는지. **변경 파일**: `TestPage.xaml`.
- 2026-07-22: **테스트 화면(TestPage) 테스트 항목에 "연결된 작업" 배지 추가** (PRD FR-E4, plan T1~T2). 사용자가 시안 렌더 이미지를 주며 "테스트↔작업 연결 배지 시안이 이미지처럼 되어 있는데 다시 확인해서 추가해놔" 요청 — 직전 작업 보고에서 "FR-E4 연결 배지 표현이 사라졌다"고 한 것에 대한 지적이었다. 시안 원본(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)을 다시 확인해 **테스트 목록 페이지의 링크 배지**(261~266줄)를 찾았고, 데이터 정의(`hasLink`/`link`, 1078줄)와 값의 출처(1508줄 `link: title` — **연결된 작업의 제목**)까지 대조했다. 즉 시안의 연결 배지는 **테스트 → 작업 방향**이며, 대장의 `[테스트→작업 역방향 링크/배지]`(과거 Phase 3에서 사용자 결정으로 제외) 항목의 재수용이다. **T1(데이터)**: `TestItem`에 `LinkedTaskTitle`(표시 전용·**비영속**) 추가 — `TodoItem.LinkedTestBadge`(작업 화면이 쓰는 반대 방향 표시 값)와 **정확히 같은 규약**을 재현했다. `TestPageViewModel.BuildLinkedTaskTitles()`가 `Rebuild()` 시작부에서 `_project.Todos`를 훑어 `LinkedTestId → 작업 제목` 딕셔너리를 만들고(중복 링크는 `TryAdd`로 첫 항목 유지) 전 테스트에 대입하는데, **연결이 없으면 빈 문자열을 항상 덮어쓴다** — 작업이 삭제되거나 링크가 끊겼을 때 옛 제목이 남지 않게 하기 위함이다(역참조를 매번 계산하므로 별도 정리 로직이 필요 없다). 값이 `TodoItem.Text`(이미 영속)에서 오므로 DB에 중복 저장하지 않았다 — 저장하면 작업 제목 변경 시 사본이 낡는다. **⚠️ 계획 리뷰가 잡아낸 결정적 결함(BLOCKER)**: `ProjectItem.Todos`는 **지연 로딩**인데(`SqliteProjectRepository.cs:35`), 테스트 화면 진입 경로 `CreateTestPageViewModel()`은 `EnsureTestsLoaded()`만 부르고 **`EnsureTodosLoaded()`를 부르지 않았다**(작업 화면 `CreateTaskPageViewModel()`은 셋 다 부른다). 이 사실을 모르고 구현했다면 딕셔너리가 항상 비어 **배지가 하나도 뜨지 않으면서 빌드·grep·리뷰를 전부 통과**했을 것이다 — 정적 검증으로는 절대 잡히지 않는 무동작이다. `EnsureTodosLoaded()` 한 줄을 추가하고 acceptance·회귀 grep으로 재발을 막았다(XML 주석도 "TestCategories와 Todos를 로드해 전달"로 갱신). **T2(마크업)**: `TestItemRowTemplate`의 행 Grid를 2단 → **3단**으로 늘려 이름(0) / **링크 배지**(1) / 메모(2) 순서를 만들고(시안 순서), 상태 아이콘·상태 pill의 `Grid.RowSpan`을 2 → **3**으로 갱신해 세로 중앙 정렬을 유지했다. 배지는 시안 값 그대로 — 테두리형 캡슐(`CornerRadius="999"`, 테두리 `#735AA3E8` = `rgba(90,163,232,.45)`의 알파 0x73 환산), 체인 아이콘(Segoe Fluent `E71B`) 10px + 작업 제목 11px, 글자 `#7AB5EC`, `Padding="9,2"`, 좌측 정렬, 넘치면 말줄임. 표시 조건은 **기존 `StringNotEmptyToVisibility` 컨버터 재사용**(함정 5는 *함수* 바인딩 한정이라 일반 `x:Bind`는 Converter를 받는다 — `TaskPage.xaml` 선례). 색은 팔레트에 같은 값이 없어 XAML 리터럴로 적었다(같은 파일 미실행 배지가 `#8A8890`/`#35353C`를 리터럴로 쓴 선례) — quality 리뷰가 근접색 `AppInfoBrush`(#5B93D8) 재사용을 SUGGEST했으나 시안 정합을 우선하고 Deferred 등재. **신규 C# 심볼 0**(마크업만), `TestPage.xaml.cs`·`Styles.xaml`·`Palette.xaml` 무변경. **검증**: 클린 리빌드 오류 0·신규 경고 0, 회귀 grep 10종 전부 기대값(행 3단·RowSpan 3×2건·글리프 1·색 리터럴 3), 전 task spec·quality 리뷰 지적 0. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: "테스트 추가"로 만든 작업↔테스트 쌍에서 배지에 작업 제목이 뜨는지 / 캡슐 모양(`CornerRadius="999"`가 완전 둥글게 렌더되는지 — 아니면 10으로 대체) / **연결된 작업을 삭제하면 배지가 사라지는지** / ⚠️ **표시 중복**: `CreateLinkedTest`가 테스트 제목을 작업 제목 그대로 복사하므로("테스트 추가" 경로) 대부분 **행 이름과 배지 텍스트가 같은 문자열로 겹쳐 보인다** — 시안 예시는 둘이 다른 데이터라 드러나지 않았던 문제로, 그대로 둘지 다르게 표현할지 사용자 판단 대상. **변경 파일**: `TestItem.cs`·`TestPageViewModel.cs`·`ProjectCardViewModel.cs`·`TestPage.xaml`.
- 2026-07-22: **작업(TaskPage) 목록 보기 화면 — 시안 원본대로 전면 재구현** (PRD FR-T2·T3·T4·T8, plan T1~T4). 사용자가 "디자인 파일 확인해서 작업 화면의 목록보기 화면 구현해" 요청 — 시안 원본(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)의 `taskViewList` 분기(143~187줄)와 데이터 소스 JS(`taskListGroups` 1405~1440줄, `colDefs`·`catColors`·`priStyles` 1198~1204줄)를 직접 읽어 사양을 추출했다. **구조가 근본부터 달랐다**: 현행은 **카테고리 1단 그룹 + 세로 카드**(상태 콤보 + 수정/삭제 버튼)인데, 시안은 **상태 그룹 4개(예정/진행 중/완료/보류) → 카테고리 서브그룹 → 가로 1줄 행**의 2단 구조다. **T1(VM)**: `TaskListStatusGroup` record(상태·라벨·개수·빈 여부·카테고리 서브그룹)와 `ListStatusGroups`·`BuildListStatusGroups`를 **추가만** 하고 기존 `CategoryGroups` 경로는 남겼다 — `x:Bind`는 컴파일 타임 바인딩이라 VM 멤버를 지우면서 XAML 소비처를 남기면 그 task에서 **반드시 XamlCompiler 오류**가 나기 때문(계획 리뷰가 이를 BLOCKER로 잡아 task 경계를 재분해했다). 카테고리 서브그룹은 칸반의 `BuildColumnGroups` 산출(`TaskColumnGroup`)을 그대로 재사용 — 정렬·배지·"미분류" 처리가 두 뷰에서 어긋나지 않으며, 대장의 `[칸반/목록 "미분류" 그룹 정렬 불일치]`도 이로써 해소됐다. **T2(XAML)**: 템플릿 3종 신설(`TaskListStatusGroupTemplate` = dot + 이름 + 개수 + 남는 폭을 채우는 구분선 + "＋ 새 작업" / `TaskListCategoryTemplate` = 칸반과 같은 카테고리 헤더 + 테스트 배지 3분기 / `TaskListRowTemplate` = 제목·설명 각 1줄 말줄임 + 날짜 범위 + 우선순위 배지), 헬퍼 4종(`StatusDotBrush`·`StatusTag`·`EmptyGroupVisibility`·`CategoriesVisibility`) 추가. 빈 상태 그룹은 **"작업 없음" 점선 박스**(함정 6: Border는 점선 미지원 → `Rectangle StrokeDashArray`), 신규 resw `TaskList_Empty.Text` ko/en(x:Uid 소비라 `.Text` 접미 — 함정 3). 구 세로 카드 경로 **14개 심볼 일괄 제거**(`TaskCardTemplate`·`CategoryGroups`·`RebuildCategoryGroups`·`TaskCategoryGroup`·`TaskStatusOption`·`StatusOptions`·`StatusCombo_*`·`EditTask_Click`·`DeleteTask_Click`·`EditTooltip`·`DeleteTooltip`·`FormatStart`·`FormatEnd`·`DateVisibility`) — VM 제거와 XAML 교체를 **같은 task에서** 수행해 빌드 성립. `TodoItem.LinkedTestBadge` 계산 경로는 **의도적으로 유지**(시안 어디에도 작업별 연결 배지가 없어 화면 표현은 사라지지만, PRD FR-E4를 되돌릴 여지를 남김 — Deferred 등재). **T3(조작)**: 목록 행에 클릭 편집·우클릭 메뉴(편집/상태 변경 4/삭제)·드래그를 **칸반과 같은 기존 핸들러 재사용**으로 배선(신규 심볼은 hover 2개뿐 — 핸들러들이 `DataContext: TodoItem`/`Tag: TodoItem`/`Tag: string` 계약만 보므로 요소 종류와 무관). 드롭 대상은 **상태 그룹 루트**에 두고 `Background="Transparent"`를 줬다 — 안쪽 목록이 받으면 항목 없는 그룹에 놓을 수 없고, 배경이 없으면 행 사이 빈 영역이 hit-test되지 않는다. hover는 `DataTemplate` 안에서 VSM `GoToState`가 안 먹어 `PointerEntered`/`PointerExited`로 테두리를 토글(TestPage 선례). **T4(색·모양)**: 시안과 실제로 **다른 색**이 드러나 사용자 확인 후 정정 — 상태 dot 예정이 코랄 `#F0716A`→회색 `#8A8890`(가장 큰 차이), 진행 중 `#5AA3E8`, 보류 `#E8925A` / 우선순위 배지 높음 `#E8B45A`, 보통 글자 `#7AB5EC`·**배경 베이스는 글자와 다른** `#5AA3E8`, 낮음 배경은 반투명→**불투명 `#2B2B31`**. 칸반 dot 5곳도 `Ellipse`(원) → `Border CornerRadius=3`(라운드 사각형)으로 통일. 이 브러시들은 칸반과 공유하므로 **칸반 화면도 함께 바뀐다**(사용자 승인). 주석을 "Palette 값과 맞춘다"에서 "**시안이 정본**"으로 갱신해 후속 수정이 팔레트 값으로 되돌리지 않게 했다. **팔레트는 무변경** — 시안 `#1f1f24`·`#27272d`·`#3d3d45` 등은 기존 브러시로 근사했고(칸반 카드가 `#25252b`를 `AppCardBrush`로 근사한 선례), `#35353C`는 Color만 있고 Brush가 없어 같은 값의 `ControlStrokeColorSecondaryBrush`를 썼다. **리뷰 특기사항**: quality 재리뷰가 `TaskEdit_Tooltip`·`TaskDelete_Tooltip` resw를 고아로 지적(MAJOR)했으나 **`TestPage.xaml.cs:60-61` + `TestPage.xaml:31,44`가 실제 소비 중**임을 근거로 반증 → 리뷰어가 오탐을 인정·철회. T2가 제거한 것은 **동명의 `TaskPage` 프로퍼티**일 뿐이며, plan이 회귀 grep 범위를 TaskPage 3파일로 한정한 이유와 같은 함정이다. **검증**: 클린 리빌드(`-t:Rebuild`) 오류 0·신규 경고 0, 회귀 grep 10종 전부 기대값, 전 task spec·quality 리뷰 통과. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 목록 전환 시 상태 그룹 4개·카테고리 서브그룹·가로 행 배치 / 빈 그룹의 "작업 없음" 점선 박스 / 행 hover 테두리(우클릭 메뉴를 닫은 뒤 잔존하지 않는지) / **행을 다른 상태 그룹으로 드래그**·빈 그룹 드롭·완료 그룹 드롭 시 작업기록 팝업 / 정정된 dot·배지 색(칸반 포함). **변경 파일**: `TaskPageViewModel.cs`·`TaskPage.xaml(.cs)`·`Strings/{ko-KR,en-US}/Resources.resw`. 브랜치 `task/taskpage-list-view` — master 병합 미실행(사용자 승인 대기).

## 미해결 이슈
- (없음)

## 주의사항
- System.Drawing.Common NuGet 패키지 추가됨 (아이콘 추출용)
- LauncherItems 테이블이 SQLite에 추가됨 (기존 DB 자동 마이그레이션)
- MainWindow 생성자에 LauncherRepository 파라미터 추가됨
- 런처 아이템은 Button 대신 Grid 사용 (CanDrag 호환성 — Button은 포인터 캡처로 드래그 불가)
- 런처 드래그&드롭: DashboardView와 동일한 DropPlaceholder 패턴 사용 (DisplayItems: ObservableCollection&lt;object&gt;)

## 아카이브 인덱스
- 2026-03.md: 동기 DB 호출 백그라운드 전환, Import 개선, 앱 안정성(전역 예외 처리)
- 2026-04.md: 런처 중복 클릭 방지·프리징 개선, 태그 마키 최적화, README·LICENSE 신규 작성
- 2026-05.md: 커맨드 스크립트 PowerShell 따옴표 손실 수정(EncodedCommand 전환)
- 2026-06.md: 테스트 목록 기능 최초 구현
- 2026-07.md: 리디자인 Phase 0~5(다크 팔레트·설정·칸반·테스트 전체 페이지·작업기록·알림), TaskEditDialog 시안 정합, To-Do/테스트 상태 모델 ComboBox 전환, 테스트 카테고리 구조
