## 최근 변경
- 2026-07-19: **리디자인 Phase 4 — 작업 기록: 유형(Kind) + 페이지네이션 + 공통 베이스 VM 정리** (PRD `docs/prd.md`, FR-H1~H4·D-5). ① **유형(Kind) 필드**(FR-H1): `HistoryEntry`에 `string Kind`(빈=미분류) POCO 프로퍼티 추가. ② **영속화 마이그레이션**(FR-H1/NFR-3): `DatabaseContext` — `AllowedIdentifiers`에 `Kind`, `MigrateSchema`에 `AddColumnIfNotExists(conn,"Histories","Kind","TEXT NOT NULL DEFAULT ''")`, `CREATE TABLE Histories`에 컬럼 추가. `SqliteProjectRepository` — `InsertHistories` INSERT 컬럼/`@kind` 파라미터 + **루프 내 값 갱신**(Phase 3 T2 @method 누락 교훈 반영, silent data loss 방지), `GetHistories`·`ReadHistoriesForProject`에 `HasColumn` 가드 read(구버전 DB 빈값). ③ **공통 베이스 VM 추출**(FR-H4/D-5): 신규 추상 `HistoryDialogViewModelBase`(ObservableObject)로 중복 로직 이전 — 검색·날짜그룹(`RebuildGroups`)·CRUD(`AddEntry`/`UpdateEntry`/`DeleteEntry`/`RefreshAfterEdit`)·마크다운 내보내기·유형(`AvailableKinds`, `BuildKinds`=DefaultHistoryKinds+settings.HistoryKinds) + **페이지네이션**(생성자 `int pageSize` 주입·`CurrentPage`·`TotalPages`·`PrevPage`/`NextPage` 커맨드·`PageInfoText`·`CanPrev/NextPage`, `RebuildGroups`가 필터→OrderBy→Skip/Take→GroupBy(날짜), 범위 클램프). `HistoryDialogViewModel`(단일)·`ProjectHistoryDialogViewModel`(다중)이 상속, 저장/프로젝트 선택만 각자 담당(추상 훅 `OnEntriesModified`·`OnEntryAdded`·`ExportProjectName`). 두 생성자에 pageSize 추가 → 호출부 4곳(`ProjectCardViewModel`·`TaskPage.xaml.cs`·`ProjectHistoryDialog.xaml.cs`·`MainWindow.xaml.cs`)이 각자 `settings.PageSize`(파생은 AppSettings 분해) 전달. `HistoryEntryViewModel`에 `Kind`·`KindVisibility` 노출. ④ **유형 선택 UI**(FR-H1/H3): 두 다이얼로그 인라인 새 기록 폼(AddPanel)에 유형 ComboBox(`AddKindCombo`, x:Uid), 코드비하인드에 `InitKindCombo`("미분류"+AvailableKinds)·`KindFromCombo`(미분류→빈) 대칭 헬퍼, 중첩 편집 다이얼로그에 유형 ComboBox → `base.UpdateEntry`에 kind 파라미터 추가·전파. ⑤ **유형 배지 + 페이지 컨트롤 restyle**(FR-H2/H3): 항목 DataTemplate에 유형 배지(Ellipse `TagColorConverter` dot + Kind 텍스트, `KindVisibility` 숨김 — 신규 컨버터 없이 작업 카드 배지 패턴 재사용), 하단 페이지 컨트롤(이전/다음 버튼 + `PageInfoText`, `HasEntries`로 숨김, 아이콘 버튼 접근성 `ToolTipService.ToolTip`). 검색·날짜그룹·펼침·내보내기·인라인폼은 기존 유지. ⑥ 다국어 resw(ko/en) 신규 키(`AddKindCombo.Header`·`HistoryKind_None`·`HistoryPage_PrevPage`·`HistoryPage_NextPage`). 빌드 MSBuild x64 오류 0. **작업 기록은 다이얼로그 유지(페이지 전환 아님 — PRD §1). 시각 렌더(유형 배지 색·정렬·페이지 컨트롤 배치)는 목업 부재로 사용자 확인 필요.** 리뷰: T3 quality MAJOR(`ProjectHistoryDialogViewModel.cs` BOM 손실→PowerShell로 복원)·T5 quality MAJOR(페이지 버튼 접근성 ToolTip 누락→PrevPageTooltip/NextPageTooltip 추가) 수정 후 전 task 통과. **미결 follow-up(Deferred)**: `HistoryEntryDialogViewModel` 소스 미참조 고아 정리(실제 편집 UI는 인라인/중첩 다이얼로그)·`InitKindCombo`/`KindFromCombo` 2곳 대칭 중복(3회째 사용 시 공용 추출)·README/스크린샷 갱신(시각 확인 후)·Todo*/Test* 구 resw 고아·`TestDateGroup` 정리(Phase 2/3 이월).
- 2026-07-19: **리디자인 Phase 3 — 테스트 전체 페이지 + 상태 모델 전환(통과/실패/미실행)** (PRD `docs/prd.md`, FR-E1~E5·D-1/D-2/D-3). ① **테스트 상태 모델 전환**(FR-E2/D-1): `TestItem` 상태 상수를 `Testing/Fix/Done` → `Pass/Fail/Untested`로 교체(비파괴 — 새 상수 추가 후 참조부 이전, 구 상수는 T6에서 구 VM/Dialog와 함께 삭제), 기본값 `Untested`, `IsCompleted => Status==Pass`, 신규 `Method`(테스트 방법) 필드. DB 값 마이그레이션 `MigrateTestStatusToNewModel`(CASE UPDATE Done→Pass·Fix→Fail·Testing→Untested, 구 값만 매칭돼 멱등)·`Method` 컬럼(`AddColumnIfNotExists`+화이트리스트)·`CREATE TABLE` DEFAULT 'Untested'·`ReadActiveTestProjectIds` `!= 'Pass'`·Repository Insert/Read Method(`HasColumn` 가드)·구버전 fallback(`isCompleted ? Pass : Untested`). ② **`TestPageViewModel`**(FR-E1/E5): 스위트(카테고리) 그룹·상태별 통계(Pass/Fail/Untested/Total)·스위트별 통과율(`TestSuiteGroup` record, 필터 무관 전체 기준)·상태 필터(`SelectedStatus`)·CRUD(스위트 추가/삭제/이름수정, 테스트 추가[`AddTestToSuite` 스위트명 매칭/생성]/편집/삭제/상태변경/메모)·FIFO 저장 체인(`QueueSave`, Phase 2 TaskPageViewModel 패턴)·`ProjectCardViewModel.CreateTestPageViewModel` 팩토리 + `RefreshTestCardState`(HasActiveTest). ③ **`TestEditDialog`**(FR-E3): 이름/스위트(ComboBox IsEditable, 기존 선택+신규 입력)/방법, `OnSave` 이름·스위트 검증, `ResultTest`+`ResultSuiteName`. 새 테스트 전용 스위트 선택(편집은 이동 미지원). ④ **`TestPage`**(FR-E1/E5): 헤더(← 대시보드·제목·상태 탭[통과/실패/미실행/전체]·새 테스트)·상태 통계 카드 3종(색 dot)·스위트 그룹(통과율 `ProgressBar`)·항목 카드(상태 아이콘/색 PRD §3: 통과 #5aa3e8 ✓·실패 #e8b45a ✕·미실행 #8a8890 ○·방법·메모·상태콤보·수정/삭제/메모). 메모 편집·삭제 확인·스위트 이름수정은 인라인 소형 ContentDialog(add/edit 경로 분리). 정적 색 브러시/글리프 헬퍼(x:Bind 함수는 Converter 미적용 → Brush/Visibility 직접 반환). ⑤ **`MapTestBadge`**(FR-E4): 작업 카드 연결 테스트 배지를 새 상수(Pass→통과/Fail→실패/Untested→미실행)로 갱신, `CreateLinkedTest` 생성 테스트 기본 미실행 승계. ⑥ **정리**(T6): 카드 "테스트" 버튼→`ShowPage(TestPage)`(다이얼로그 대체, Phase 2 페이지 네비 재사용), 구 `TestListDialog`/`TestListDialogViewModel` 3파일 삭제·csproj Page 항목 제거·`TestChanged`/`OnTestChanged` 이벤트 체인 제거(증분 저장이 승계)·`TestItem` 구 상수 제거. ⑦ 다국어 resw(ko/en) 신규 문자열(TestEdit_*·TestPage_*·TestStatus_* 등 17키). 빌드 MSBuild x64 오류 0·신규 경고 0(구 TestListDialog WMC1506 경고도 소멸). **시각 렌더(통계 카드·상태 탭·스위트 통과율·상태 아이콘 레이아웃)·영속 왕복[상태 변경→재열기]·구 데이터 마이그레이션은 사용자 확인 필요**(리디자인 목업 부재 → PRD §3 추출값 기준). 리뷰: T2 quality MAJOR(InsertTestItems `@method` 루프 바인딩 누락→silent data loss) 수정, T4 quality MAJOR(주석 코드와 반대) 수정, T5 spec BLOCKER(plan T5 acceptance 문서 정정으로 해소) 후 전 task 통과. 교차 집계(D-2)·테스트→작업 역방향 링크(FR-E4 확장)는 Deferred. 미결 follow-up: Todo*/Test* 구 resw 고아 정리(대량)·`TestDateGroup` 고아 정리·미사용 심볼(ShowKanban 등).
- 2026-07-19: **리디자인 Phase 2 — 작업(칸반) 전체 페이지 + 페이지 네비게이션** (PRD `docs/prd.md`, FR-C3·FR-T1~T6·T8). ① **페이지 네비게이션 컨테이너**(FR-C3): `MainWindow.ShowPage(UIElement)`/`ShowDashboard()` — `DashboardContent` 콘텐츠 스왑 + `GroupTabsBar`(신규 x:Name) Visibility 토글, DashboardView 인스턴스 보관(재생성 회피), Frame 미도입(기존 idiom). ② **작업 도메인 확장**(FR-T1): `TodoStatus`에 `Hold`(보류), 신규 `TaskPriority`(High/Normal/Low), `TodoItem`에 Category·Priority·StartDate·EndDate·LinkedTestId + Todos 테이블 5컬럼 하위호환 마이그레이션(`AddColumnIfNotExists`+`AllowedIdentifiers`, InsertTodos·GetTodos·ReadTodosForProject·`ReadNullableDateColumn` 헬퍼). ③ **`TaskPageViewModel`**: 칸반 4열(상태별 ObservableCollection)·상태별 카운트·카테고리 필터·칸반/목록 뷰 전환·`MoveToStatus`·**FIFO 저장 체인**(`QueueSave`/`_saveChain.ContinueWith`+스냅샷+try/catch — 연속 변경 유실·예외 무시 방지)·완료→작업기록 팝업 훅(`WorkLogRequested`→`CommitWorkLog`→`SaveHistories`). ④ **`TaskPage`**(FR-T2/T4): 헤더(뒤로가기·뷰전환·카테고리 필터·새 작업)+칸반 4열/목록 그룹+카드(카테고리 dot[TagColor]·우선순위·시작/종료일·상태콤보·수정/삭제·테스트 배지). ⑤ **드래그앤드롭**(FR-T3): 카드 CanDrag·4열 드롭 타깃(Tag=상태)→`MoveToStatus`. ⑥ **새 작업/편집 다이얼로그**(FR-T5): `TaskEditDialog`(6필드+"테스트 추가" 토글, 새 작업 전용). ⑦ **작업↔테스트 연결·배지**(FR-T6/T8): `CreateLinkedTest`("작업" TestCategory 자동생성/재사용·`LinkedTestId`), 카드 테스트 상태 배지(`LinkedTestBadge` transient·`MapTestBadge`). ⑧ **정리**: 카드 "작업" 버튼→페이지 네비(다이얼로그 대체), 구 `TodoDialog`/`TodoDialogViewModel`/`TodoDetailDialogViewModel` 4파일 삭제 + `TodoChanged` 이벤트 체인 제거(HistoryChanged·완료훅 보존). ⑨ 다국어 resw(ko/en) 신규 문자열. 빌드 MSBuild x64 오류 0·신규 경고 0. **시각 렌더(칸반 레이아웃·드래그 UX·배지)·영속 왕복은 사용자 확인 필요**(리디자인 목업 부재 → PRD §3 추출값 기준). 미결 follow-up: Todo* resw 고아 정리(대량)·프로젝트별만(교차 집계 Deferred)·담당자(FR-T7) 제외·테스트 배지 Phase 3 모델 매핑.
- 2026-07-18: **리디자인 Phase 1 — 설정(페이지당 표시 개수·작업 카테고리·작업 기록 유형·테마 셀렉터 제거)** (PRD `docs/prd.md`). ① `AppSettings`에 `PageSize`(기본 100)·`TaskCategories`·`HistoryKinds`(List<string>) 추가 — `AppJsonContext` 소스제너레이터로 JSON 자동 직렬화, `AppSettingsDialogViewModel.LoadFrom`/`ApplyTo`에 매핑(다이얼로그 close 시 유실 방지). ② 앱 설정 코드 메뉴에 "작업 카테고리"(기본 UI·UX/프론트엔드/백엔드)·"작업 기록 유형"(기본 기능/수정/리팩터링/문서) 관리 블록 추가 — 기존 기술스택 태그 패턴(`TagDisplayItem`·`Default*`·`Add/Remove`·`CanAdd`·`WrapPanel`·IsDefault 삭제버튼 숨김) 복제. ③ 설정 메뉴에 페이지당 표시 개수 `NumberBox`(100~1000, step 10). ④ **테마 셀렉터 제거**(다크 단일 고정) — VM `ThemeModeItems`/`SelectedThemeModeItem`/`OnSelectedThemeModeItemChanged`/`ThemeModeItem` record + XAML `ThemeCard` + 코드비하인드 핸들러(`_vmPropertyChangedHandler` 필드·구독·해제) + 고아 `ToElementTheme`·`ThemeModeRadioConverter`(파일·Converters.xaml 등록) + resw `ThemeCard.*`/`Theme_*` 제거. `AppSettings.ThemeMode`/enum/`ApplyTheme`는 휴면 유지(직렬화·시그니처 안전). ⑤ 다국어 resw(ko/en) 신규 문자열 추가. FR-S5(프로젝트 설정)는 현재 다이얼로그가 시안보다 필드 많고 Phase 0 팔레트로 이미 다크라 구조 재설계는 기능 손실 위험 → Deferred. 빌드 MSBuild x64 오류 0·신규 경고 0. **시각 렌더·영속 왕복은 사용자 확인 필요.**
- 2026-07-18: **리디자인 Phase 0 — 다크 팔레트 통일·전역 폰트** (claude.ai/design 시안 반영, PRD `docs/prd.md`). ① `Resources/Palette.xaml` 신규 — `ThemeDictionaries["Default"]`에 디자인 다크 팔레트를 Color+Brush 동시 정의로 오버라이드(개별 화면 XAML 무수정 일괄 반영). 직접참조 시스템 브러시 20개(TextFillColor*, CardBackgroundFillColor*, SolidBackgroundFillColor*, Accent*, Control*, SystemFillColor* 등) + 표준 컨트롤 크롬(ContentDialog*/TextControl*/Button*/ComboBox*/Layer*)을 디자인 값(배경 #131316·카드 #1c1c20·강조 코랄 #f0716a·본문 #e8e6e3 등)으로. 재사용 토큰(`App*Color`/`App*Brush`)도 정의(이후 Phase 재사용). ② 전역 다크 고정 — `AppSettingsDialogViewModel.ApplyTheme`/`ToElementTheme`를 저장된 ThemeMode와 무관하게 항상 `ElementTheme.Dark` 반환/적용(시그니처 유지, 호출부 3경로 일괄 커버: App.xaml.cs 시작·설정 변경·AppSettingsDialog 자체). ③ 전역 기본 폰트 Malgun Gothic(`App.xaml`의 `ContentControlThemeFontFamily` 오버라이드, 아이콘 Segoe MDL2·코드 Consolas 유지). ④ `TagColorConverter` HSL 채도 0.55→0.45·명도 0.30→0.36으로 다크 조화. 빌드 MSBuild x64 오류 0·신규 경고 0(기존 7 경고는 NU1903/Icon obsolete/OneWay 바인딩 — 무관). **시각 렌더는 사용자 확인 필요**(테마 셀렉터 UI 제거·AppSettings.ThemeMode 정리는 Phase 1, 페이지 네비 컨테이너는 Phase 2로 이연).
- 2026-05-04: 커맨드 스크립트 PowerShell 실행 시 따옴표(`"`) 손실 문제 수정 — `ProjectCardViewModel.LaunchCommandScript`에서 PowerShell 분기를 `-Command "..."` 래핑 방식에서 `-EncodedCommand`(Base64 UTF-16LE) 방식으로 교체. 외부 따옴표가 사라져 사용자 스크립트 내 `"`가 그대로 전달됨(예: `git commit -m "fix bug"` 정상 동작). 작업 폴더 이동을 `Set-Location -LiteralPath '경로'` 로 강화하여 와일드카드/대괄호가 포함된 폴더 경로도 안전하게 처리(경로 내 작은따옴표는 두 번으로 이스케이프). `using System.Text;` 추가. Cmd 분기/시그니처/UI 변경 없음
- 2026-04-26: README.md 리뷰 보강 — (1) 다국어 표기를 본문/설정 섹션 모두 "시스템 기본 / 한국어 / English (재시작 후 반영)"로 통일, (2) 빌드 결과 경로를 `bin/{Platform}/{Configuration}/net10.0-windows10.0.26100.0/` 일반형 + Debug/Release 예시로 보강, (3) 아이콘 캐시 표 항목을 실제 경로(`LauncherIcons\` 폴더 + `launcher_icon_cache.json` 인덱스, LRU 500개·20% 정리)로 명시. 코드 변경 없음
- 2026-04-26: 솔루션 루트에 `LICENSE` (MIT, Copyright © 2026 JongCheol Pak) 추가, `README.md` 라이선스 섹션을 `[MIT License](LICENSE) 하에 배포됩니다.` 로 갱신
- 2026-04-26: 솔루션 루트 `README.md` 신규 작성 — MailTrayNotifier README 스타일을 따라 헤더 아이콘(`docs/screenshots/appicon.png`) + 주요 기능(카드/실행 액션/To-Do·테스트·기록/런처/설정) + 설치(MSBuild x64) + 사용 방법(스크린샷 6종 임베드) + 설정 파일 위치 + 주요 의존성 + 제한 사항 + 라이선스 섹션으로 구성. 이미지 경로는 솔루션 루트 기준 `docs/screenshots/`. 코드/기능 변경은 없음 (문서 신규 작성)
- 2026-04-20: 런처 중복 클릭 방지 (커맨드 레벨) — `Launch`/`LaunchAsAdmin`을 `async Task` + `[RelayCommand(AllowConcurrentExecutions = false)]`로 전환. 내부 구현은 `LaunchAppAsync`로 분리하고 `Task.Run` 이후 `Task.Delay(800ms)` 쿨다운을 두어 `Process.Start`가 즉시 리턴되는 케이스에서도 더블클릭/연타 시 추가 실행이 차단됨. 커맨드 이름(`LaunchCommand`/`LaunchAsAdminCommand`) 유지로 `MainWindow.xaml.cs` 호출부 변경 불필요
- 2026-04-20: 런처 앱 실행 UI 프리징 개선 — `LauncherViewModel.LaunchApp`에서 `Process.Start(UseShellExecute=true)` 호출을 `Task.Run`으로 감싸 백그라운드 스레드에서 수행. UAC 프롬프트/shell 초기화/느린 앱 로딩 시 UI 스레드 블록 방지. Fire-and-forget 패턴, 공개 API 시그니처 변경 없음
- 2026-04-20: 태그 마키 CPU/GPU 부하 최적화 — `MarqueeTagsControl` 내부 로직만 수정. 평상시에는 정적 트리밍 + "+N" 배지로 표시하고, 포인터가 **카드 전체** 영역에 올라온 경우에만 마키 재생 (Loaded 시 시각 트리에서 DataContext 경계로 카드 루트를 찾아 PointerEntered/Exited 구독, Unloaded 시 해제). `EffectiveViewportChanged`로 뷰포트 밖(스크롤로 가려진) 카드는 자동 정지, `UISettings.AnimationsEnabled=false`면 재생 억제. 공개 API/DP 시그니처·DashboardView 바인딩 변경 없음
- 2026-07-02: To-Do 상태 모델 변경 — CheckBox(완료/미완료) → ComboBox 3상태(대기/진행 중/완료)로 전환, 탭 4개(대기/진행 중/완료/전체)로 변경, 탭에 항목 개수 표시, TodoItem.Status 프로퍼티 추가, DB Todos.Status 컬럼 마이그레이션, 새 할 일 입력을 대기 탭으로 이동, "완료됨" → "완료" 문구 변경
- 2026-07-01: 테스트 목록 상태 모델 변경 — CheckBox(완료/미완료) → ComboBox 3상태(테스트/수정/완료)로 전환, 탭 4개(테스트/수정/완료/전체), 전체 탭에서 수정 항목 빨간색 표시, TestItem.IsCompleted → Status 프로퍼티 변경, DB Status 컬럼 마이그레이션
- 2026-07-01: 테스트 목록 카테고리 기반 구조 전환 — TestCategory 엔티티 추가, TestItem에 CategoryId 필드 추가, ProjectItem.Tests → TestCategories로 변경, DB 스키마(TestCategories 테이블/마이그레이션), IProjectRepository/SqliteProjectRepository 카테고리 CRUD, TestListDialogViewModel 카테고리 기반 재작성, TestListDialog XAML/코드비하인드 카테고리 카드 UI, 다국어 리소스 추가
- 2026-06-30: 테스트 목록 기능 구현 — 프로젝트 카드별 테스트 항목 관리 다이얼로그 추가 (TestItem 엔티티, TestItems DB 테이블, TestListDialog, 진행 내용(ProgressNote) 편집, 탭 필터링/날짜 그룹화, 중첩 다이얼로그 지원)
- 2026-03-20: 모든 동기 DB 호출 백그라운드 전환
- 2026-03-20: 가져오기(Import) 전면 개선 — DB 읽기/쓰기 Task.Run 백그라운드 실행(UI 프리징 방지), 중복 체크 일괄 수행, 런처 아이콘 추출 병렬화(최대 4개 동시), UWP 미설치 앱 PackageManager 필터링, null ExecutablePath 가드
- 2026-03-20: 앱 안정성 개선 — UnhandledException/UnobservedTaskException 전역 핸들러 추가, DialogService 동시 표시 방지(SemaphoreSlim), ContentDialog 예외 안전 처리, Process 객체 미해제 수정(LauncherViewModel/VersionCheckService), PropertyChanged 람다 및 Timer Tick 핸들러 해제 추가, NullToVisibilityConverter 주석 수정, 아이콘 캐시 크기 제한(500) 추가

## 미해결 이슈
- (없음)

## 주의사항
- System.Drawing.Common NuGet 패키지 추가됨 (아이콘 추출용)
- LauncherItems 테이블이 SQLite에 추가됨 (기존 DB 자동 마이그레이션)
- MainWindow 생성자에 LauncherRepository 파라미터 추가됨
- 런처 아이템은 Button 대신 Grid 사용 (CanDrag 호환성 — Button은 포인터 캡처로 드래그 불가)
- 런처 드래그&드롭: DashboardView와 동일한 DropPlaceholder 패턴 사용 (DisplayItems: ObservableCollection&lt;object&gt;)
