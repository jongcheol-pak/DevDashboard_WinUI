## 최근 변경
- 2026-07-22: **상단 "새 프로젝트" 버튼 제거 + 카드 목록의 "새 프로젝트 추가" 카드를 항상 첫 번째로**. 사용자 요청 — 프로젝트 추가 진입점을 헤더 버튼과 카드 두 곳에서 **카드 하나로 일원화**하고, 그 카드가 목록 끝이 아니라 맨 앞에 오게 했다. **① 헤더 버튼 제거**: `MainWindow.xaml`의 AccentButton(`x:Uid="NewProjectText"`)과 소비처가 사라진 `AddProjectButton_Click` 핸들러를 함께 삭제 — 같은 동작의 `OnAddProjectRequested`(카드 → VM 이벤트 경로)가 남아 있어 기능 손실은 없다. **② 카드 순서**: `MainViewModel.ApplyFilterAndSort`의 `DisplayCards.ResetWith(sorted.Cast<object>().Append(_addCardPlaceholder))`를 `Prepend(...)`로 바꿔 플레이스홀더를 선두에 놓고, `DisplayCards` XML 주석도 순서에 맞게 갱신. **드래그&드롭 영향 없음** — `DashboardView.xaml.cs`의 드롭 플레이스홀더는 `DisplayCards.IndexOf(target)`로 위치를 잡는 상대 인덱스 방식이라 앞에 항목이 하나 늘어도 계산이 어긋나지 않는다. **미사용 잔존**: resw 키 `NewProjectText.Text`(ko/en)는 소비처가 없어졌으나 리소스 삭제는 요청 범위 밖이라 그대로 뒀다. **검증**: MSBuild x64 Debug 빌드 오류 0·신규 경고 0(기존 `XamlTypeInfo.g.cs` CS0618 1건만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 헤더에서 버튼이 사라지고 우측 정렬이 어색하지 않은지 / 추가 카드가 목록 맨 앞에 뜨고 클릭 시 등록 다이얼로그가 열리는지 / 고정 카드 드래그 재정렬이 정상인지. **변경 파일**: `MainWindow.xaml(.cs)`·`Presentation/ViewModels/MainViewModel.cs`.
- 2026-07-22: **테스트 항목 행 — 이름이 행 위쪽에 붙던 문제(세로 중앙 정렬)**. 사용자가 "제목만 있는 경우 위쪽에 표시된다, 세로 중앙으로" 지적. **원인**: 직전 작업(연결 배지)에서 행 Grid를 3단(이름/배지/메모)으로 만들면서, 배지·메모가 없는 항목은 Row 1·2가 비고 **`Grid.RowSpan="3"`인 상태 아이콘·상태 pill이 만드는 남는 높이가 빈 아래 행으로 몰려** 이름만 위로 붙었다(전체 창 스크린샷에서 pill이 이름보다 살짝 아래에 찍힌 것이 근거). 처음엔 `MaxLines="2"`가 한 줄 텍스트에도 2줄 높이를 예약하는 것으로 의심해 임시 제거·빌드했으나 사용자 실행 확인 결과 **변화 없어 가설 기각**, 원래대로 복원했다. **수정**: 이름·배지·메모를 행마다 `Grid.Row`로 쪼개는 대신 **하나의 `StackPanel`로 묶고 그 묶음에 `VerticalAlignment="Center"`**를 줬다 — 행 높이가 무엇 때문에 커지든 항상 세로 중앙이 된다. 이에 따라 `Grid.RowDefinitions`(3개)와 상태 아이콘·pill의 `Grid.RowSpan="3"`을 제거(RowSpan이 없어도 단일 행이라 세로 중앙 유지). 배지 내부 `Grid`(폭을 내용에 맞추는 Auto 2열)는 그대로 — 세로 StackPanel은 자식에게 가로 폭을 제한해 주므로 긴 제목의 말줄임 동작에 영향이 없다. **검증**: 빌드 오류 0·신규 경고 0(기존 `XamlTypeInfo.g.cs` CS0618만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 제목만 있는 행에서 이름·상태 아이콘·pill이 같은 높이로 보이는지 / 배지·메모가 있는 행의 세로 배치가 깨지지 않았는지. **변경 파일**: `TestPage.xaml`.
- 2026-07-22: **테스트 화면(TestPage) 테스트 항목에 "연결된 작업" 배지 추가** (PRD FR-E4, plan T1~T2). 사용자가 시안 렌더 이미지를 주며 "테스트↔작업 연결 배지 시안이 이미지처럼 되어 있는데 다시 확인해서 추가해놔" 요청 — 직전 작업 보고에서 "FR-E4 연결 배지 표현이 사라졌다"고 한 것에 대한 지적이었다. 시안 원본(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)을 다시 확인해 **테스트 목록 페이지의 링크 배지**(261~266줄)를 찾았고, 데이터 정의(`hasLink`/`link`, 1078줄)와 값의 출처(1508줄 `link: title` — **연결된 작업의 제목**)까지 대조했다. 즉 시안의 연결 배지는 **테스트 → 작업 방향**이며, 대장의 `[테스트→작업 역방향 링크/배지]`(과거 Phase 3에서 사용자 결정으로 제외) 항목의 재수용이다. **T1(데이터)**: `TestItem`에 `LinkedTaskTitle`(표시 전용·**비영속**) 추가 — `TodoItem.LinkedTestBadge`(작업 화면이 쓰는 반대 방향 표시 값)와 **정확히 같은 규약**을 재현했다. `TestPageViewModel.BuildLinkedTaskTitles()`가 `Rebuild()` 시작부에서 `_project.Todos`를 훑어 `LinkedTestId → 작업 제목` 딕셔너리를 만들고(중복 링크는 `TryAdd`로 첫 항목 유지) 전 테스트에 대입하는데, **연결이 없으면 빈 문자열을 항상 덮어쓴다** — 작업이 삭제되거나 링크가 끊겼을 때 옛 제목이 남지 않게 하기 위함이다(역참조를 매번 계산하므로 별도 정리 로직이 필요 없다). 값이 `TodoItem.Text`(이미 영속)에서 오므로 DB에 중복 저장하지 않았다 — 저장하면 작업 제목 변경 시 사본이 낡는다. **⚠️ 계획 리뷰가 잡아낸 결정적 결함(BLOCKER)**: `ProjectItem.Todos`는 **지연 로딩**인데(`SqliteProjectRepository.cs:35`), 테스트 화면 진입 경로 `CreateTestPageViewModel()`은 `EnsureTestsLoaded()`만 부르고 **`EnsureTodosLoaded()`를 부르지 않았다**(작업 화면 `CreateTaskPageViewModel()`은 셋 다 부른다). 이 사실을 모르고 구현했다면 딕셔너리가 항상 비어 **배지가 하나도 뜨지 않으면서 빌드·grep·리뷰를 전부 통과**했을 것이다 — 정적 검증으로는 절대 잡히지 않는 무동작이다. `EnsureTodosLoaded()` 한 줄을 추가하고 acceptance·회귀 grep으로 재발을 막았다(XML 주석도 "TestCategories와 Todos를 로드해 전달"로 갱신). **T2(마크업)**: `TestItemRowTemplate`의 행 Grid를 2단 → **3단**으로 늘려 이름(0) / **링크 배지**(1) / 메모(2) 순서를 만들고(시안 순서), 상태 아이콘·상태 pill의 `Grid.RowSpan`을 2 → **3**으로 갱신해 세로 중앙 정렬을 유지했다. 배지는 시안 값 그대로 — 테두리형 캡슐(`CornerRadius="999"`, 테두리 `#735AA3E8` = `rgba(90,163,232,.45)`의 알파 0x73 환산), 체인 아이콘(Segoe Fluent `E71B`) 10px + 작업 제목 11px, 글자 `#7AB5EC`, `Padding="9,2"`, 좌측 정렬, 넘치면 말줄임. 표시 조건은 **기존 `StringNotEmptyToVisibility` 컨버터 재사용**(함정 5는 *함수* 바인딩 한정이라 일반 `x:Bind`는 Converter를 받는다 — `TaskPage.xaml` 선례). 색은 팔레트에 같은 값이 없어 XAML 리터럴로 적었다(같은 파일 미실행 배지가 `#8A8890`/`#35353C`를 리터럴로 쓴 선례) — quality 리뷰가 근접색 `AppInfoBrush`(#5B93D8) 재사용을 SUGGEST했으나 시안 정합을 우선하고 Deferred 등재. **신규 C# 심볼 0**(마크업만), `TestPage.xaml.cs`·`Styles.xaml`·`Palette.xaml` 무변경. **검증**: 클린 리빌드 오류 0·신규 경고 0, 회귀 grep 10종 전부 기대값(행 3단·RowSpan 3×2건·글리프 1·색 리터럴 3), 전 task spec·quality 리뷰 지적 0. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: "테스트 추가"로 만든 작업↔테스트 쌍에서 배지에 작업 제목이 뜨는지 / 캡슐 모양(`CornerRadius="999"`가 완전 둥글게 렌더되는지 — 아니면 10으로 대체) / **연결된 작업을 삭제하면 배지가 사라지는지** / ⚠️ **표시 중복**: `CreateLinkedTest`가 테스트 제목을 작업 제목 그대로 복사하므로("테스트 추가" 경로) 대부분 **행 이름과 배지 텍스트가 같은 문자열로 겹쳐 보인다** — 시안 예시는 둘이 다른 데이터라 드러나지 않았던 문제로, 그대로 둘지 다르게 표현할지 사용자 판단 대상. **변경 파일**: `TestItem.cs`·`TestPageViewModel.cs`·`ProjectCardViewModel.cs`·`TestPage.xaml`.
- 2026-07-22: **작업(TaskPage) 목록 보기 화면 — 시안 원본대로 전면 재구현** (PRD FR-T2·T3·T4·T8, plan T1~T4). 사용자가 "디자인 파일 확인해서 작업 화면의 목록보기 화면 구현해" 요청 — 시안 원본(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)의 `taskViewList` 분기(143~187줄)와 데이터 소스 JS(`taskListGroups` 1405~1440줄, `colDefs`·`catColors`·`priStyles` 1198~1204줄)를 직접 읽어 사양을 추출했다. **구조가 근본부터 달랐다**: 현행은 **카테고리 1단 그룹 + 세로 카드**(상태 콤보 + 수정/삭제 버튼)인데, 시안은 **상태 그룹 4개(예정/진행 중/완료/보류) → 카테고리 서브그룹 → 가로 1줄 행**의 2단 구조다. **T1(VM)**: `TaskListStatusGroup` record(상태·라벨·개수·빈 여부·카테고리 서브그룹)와 `ListStatusGroups`·`BuildListStatusGroups`를 **추가만** 하고 기존 `CategoryGroups` 경로는 남겼다 — `x:Bind`는 컴파일 타임 바인딩이라 VM 멤버를 지우면서 XAML 소비처를 남기면 그 task에서 **반드시 XamlCompiler 오류**가 나기 때문(계획 리뷰가 이를 BLOCKER로 잡아 task 경계를 재분해했다). 카테고리 서브그룹은 칸반의 `BuildColumnGroups` 산출(`TaskColumnGroup`)을 그대로 재사용 — 정렬·배지·"미분류" 처리가 두 뷰에서 어긋나지 않으며, 대장의 `[칸반/목록 "미분류" 그룹 정렬 불일치]`도 이로써 해소됐다. **T2(XAML)**: 템플릿 3종 신설(`TaskListStatusGroupTemplate` = dot + 이름 + 개수 + 남는 폭을 채우는 구분선 + "＋ 새 작업" / `TaskListCategoryTemplate` = 칸반과 같은 카테고리 헤더 + 테스트 배지 3분기 / `TaskListRowTemplate` = 제목·설명 각 1줄 말줄임 + 날짜 범위 + 우선순위 배지), 헬퍼 4종(`StatusDotBrush`·`StatusTag`·`EmptyGroupVisibility`·`CategoriesVisibility`) 추가. 빈 상태 그룹은 **"작업 없음" 점선 박스**(함정 6: Border는 점선 미지원 → `Rectangle StrokeDashArray`), 신규 resw `TaskList_Empty.Text` ko/en(x:Uid 소비라 `.Text` 접미 — 함정 3). 구 세로 카드 경로 **14개 심볼 일괄 제거**(`TaskCardTemplate`·`CategoryGroups`·`RebuildCategoryGroups`·`TaskCategoryGroup`·`TaskStatusOption`·`StatusOptions`·`StatusCombo_*`·`EditTask_Click`·`DeleteTask_Click`·`EditTooltip`·`DeleteTooltip`·`FormatStart`·`FormatEnd`·`DateVisibility`) — VM 제거와 XAML 교체를 **같은 task에서** 수행해 빌드 성립. `TodoItem.LinkedTestBadge` 계산 경로는 **의도적으로 유지**(시안 어디에도 작업별 연결 배지가 없어 화면 표현은 사라지지만, PRD FR-E4를 되돌릴 여지를 남김 — Deferred 등재). **T3(조작)**: 목록 행에 클릭 편집·우클릭 메뉴(편집/상태 변경 4/삭제)·드래그를 **칸반과 같은 기존 핸들러 재사용**으로 배선(신규 심볼은 hover 2개뿐 — 핸들러들이 `DataContext: TodoItem`/`Tag: TodoItem`/`Tag: string` 계약만 보므로 요소 종류와 무관). 드롭 대상은 **상태 그룹 루트**에 두고 `Background="Transparent"`를 줬다 — 안쪽 목록이 받으면 항목 없는 그룹에 놓을 수 없고, 배경이 없으면 행 사이 빈 영역이 hit-test되지 않는다. hover는 `DataTemplate` 안에서 VSM `GoToState`가 안 먹어 `PointerEntered`/`PointerExited`로 테두리를 토글(TestPage 선례). **T4(색·모양)**: 시안과 실제로 **다른 색**이 드러나 사용자 확인 후 정정 — 상태 dot 예정이 코랄 `#F0716A`→회색 `#8A8890`(가장 큰 차이), 진행 중 `#5AA3E8`, 보류 `#E8925A` / 우선순위 배지 높음 `#E8B45A`, 보통 글자 `#7AB5EC`·**배경 베이스는 글자와 다른** `#5AA3E8`, 낮음 배경은 반투명→**불투명 `#2B2B31`**. 칸반 dot 5곳도 `Ellipse`(원) → `Border CornerRadius=3`(라운드 사각형)으로 통일. 이 브러시들은 칸반과 공유하므로 **칸반 화면도 함께 바뀐다**(사용자 승인). 주석을 "Palette 값과 맞춘다"에서 "**시안이 정본**"으로 갱신해 후속 수정이 팔레트 값으로 되돌리지 않게 했다. **팔레트는 무변경** — 시안 `#1f1f24`·`#27272d`·`#3d3d45` 등은 기존 브러시로 근사했고(칸반 카드가 `#25252b`를 `AppCardBrush`로 근사한 선례), `#35353C`는 Color만 있고 Brush가 없어 같은 값의 `ControlStrokeColorSecondaryBrush`를 썼다. **리뷰 특기사항**: quality 재리뷰가 `TaskEdit_Tooltip`·`TaskDelete_Tooltip` resw를 고아로 지적(MAJOR)했으나 **`TestPage.xaml.cs:60-61` + `TestPage.xaml:31,44`가 실제 소비 중**임을 근거로 반증 → 리뷰어가 오탐을 인정·철회. T2가 제거한 것은 **동명의 `TaskPage` 프로퍼티**일 뿐이며, plan이 회귀 grep 범위를 TaskPage 3파일로 한정한 이유와 같은 함정이다. **검증**: 클린 리빌드(`-t:Rebuild`) 오류 0·신규 경고 0, 회귀 grep 10종 전부 기대값, 전 task spec·quality 리뷰 통과. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 목록 전환 시 상태 그룹 4개·카테고리 서브그룹·가로 행 배치 / 빈 그룹의 "작업 없음" 점선 박스 / 행 hover 테두리(우클릭 메뉴를 닫은 뒤 잔존하지 않는지) / **행을 다른 상태 그룹으로 드래그**·빈 그룹 드롭·완료 그룹 드롭 시 작업기록 팝업 / 정정된 dot·배지 색(칸반 포함). **변경 파일**: `TaskPageViewModel.cs`·`TaskPage.xaml(.cs)`·`Strings/{ko-KR,en-US}/Resources.resw`. 브랜치 `task/taskpage-list-view` — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **작업(TaskPage) 칸반 카테고리 헤더의 통과율 배지 — 시안 원본과 대조해 계산식·색상 재정정**. 사용자가 "통과율 표시 디자인 확인해서 동일하게 수정" 요청 — 디자인 원본(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)의 `kanbanCols` 빌드 JS(1223~1238줄)를 찾아 실제 계산식·스타일을 대조했다. **드러난 차이 2건**: ① **통과율 계산식이 틀려 있었다** — 기존 `BuildPassRateBadge`는 `pass / **total**`(미실행 포함 전체 건수)로 나눴지만, 시안은 `passed / **ran.length**`(미실행을 뺀 **실행된 건수**)로 나눈다. 예: 4건 중 2통과·1실패·1미실행이면 시안은 67%, 기존 코드는 50%로 다른 숫자가 나온다. ② **색 분기가 2단계뿐이었다** — 기존은 "테스트 없음/전부 미실행=회색" / "그 외엔 결과가 몇 %든 무조건 초록"이라 통과율이 낮아도 무조건 초록으로 보였다. 시안은 3단계: 미실행(회색 **테두리만**, 배경 없음, 보통 굵기) / 실행됐지만 **100% 미만**(호박 채움+굵게) / **100% 통과**(초록 채움+굵게). **수정**: `TaskPageViewModel.BuildPassRateBadge`가 `executed`(미실행 제외 건수)로 나누도록 분모 교체, 반환 튜플에 `IsFullPass`(반올림 통과율이 100 이상) 추가 → `TaskColumnGroup` record에 `IsFullPass` 멤버 추가(호출부 1곳). `TaskPage.xaml`의 배지 `Border` 1개를 시안의 두 `<span>` 분기 구조 그대로 **2개**(미실행용 테두리형 / 실행용 채움형, `IdleTestBadgeVisibility`/`RanTestBadgeVisibility`로 상호 배타 표시)로 나누고, 실행용 텍스트에 `FontWeight="Bold"` 추가. `TaskPage.xaml.cs`의 `TestBadgeForeground`/`TestBadgeBackground`를 `bool hasTestResult` 대신 `bool isFullPass`로 받아 초록/호박 2색만 반환하도록 교체(신규 `_testBadgeAmberBrush`/`_testBadgeAmberSoftBrush`, 시안 hex `#e8b45a`·0x28 알파 관례 그대로), 미실행 배지는 색 분기가 필요 없어 XAML에 `#8A8890`/`#35353C` 리터럴로 직접 기술. **범위 결정**: 시안에는 목록 뷰(카테고리별 그룹 헤더)에도 같은 배지가 있으나(`cg.testBadgeStyle`), 사용자가 "칸반만 수정(색상·계산식만)"으로 범위를 좁혀 목록 뷰 추가는 이번에 하지 않음 — 필요해지면 별도 요청. **검증**: 빌드 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). 시각 최종 확인(호박/초록 색 구분, 미실행 테두리형 배지 모양)은 사용자 몫. **변경 파일**: `TaskPageViewModel.cs`·`TaskPage.xaml(.cs)`.
- 2026-07-21: **테스트 목록(TestPage) 상태 아이콘 — 시안 원본(HTML/JS)과 대조해 재정정**. 사용자가 렌더 스크린샷과 시안이 "너무 다르다"고 재지적하며 이번엔 시안 원본 파일(`docs/DevDashboard WinUI/DevDashboard Redesign.dc.html`)을 직접 지목 — claude.ai/design 링크는 인증 필요(403)라 WebFetch로 열람 불가했으나, 로컬 export 파일에서 상태 아이콘을 실제로 그리는 JS(`statusMeta`, 1046~1082줄)를 확인해 **텍스트 묘사·렌더 판정이 아닌 소스 그대로**의 사양을 얻었다. **드러난 사실**: 직전(같은 날 앞선 항목) 재지적으로 확정했던 D3("미실행만 CornerRadius=11 원형·테두리만·글리프 없음")가 **시안과 다시 어긋나 있었다** — 실제 시안은 **세 상태(통과/실패/미실행) 전부 동일한 라운드 사각형**(`20×20`, `border-radius:6`)이고, 통과·실패는 진한 단색 채움이 아니라 **연한 색조(rgba 14% 알파) 배경 + 색이 들어간 글리프**(흰색 아님), 미실행은 **불투명 회색(#2b2b31) 배경 + 문자 그대로 "○" 글리프**(빈 원 테두리가 아니다)였다. 즉 이전의 "사각 테두리 안에 원 글리프가 겹쳐 보인다"는 렌더 재지적은 실제로는 **채움 방식(진한 단색+흰 글리프)이 시안과 달라서 생긴 부작용**이었지, 미실행의 모양 자체를 원형으로 바꿔야 한다는 뜻이 아니었다. **수정**: `TestPage.xaml.cs` — 아이콘 배경 헬퍼 `StatusIconBackground`를 통과/실패는 기존 pill과 같은 `_passSoftBrush`/`_failSoftBrush`(0x28 알파, 이미 정의돼 있던 것 재사용) 반환으로, 미실행은 신규 `_untestedFillBrush`(불투명 `#2B2B31`, Palette `SubtleFillColorSecondary`와 동일값)로 교체. `StatusGlyph`의 미실행을 `string.Empty` → `"○"`로 복원. 이제 형태 분기가 없어져 `StatusIconBorderBrush`·`StatusIconBorderThickness`·`StatusIconCornerRadius`·`StatusGlyphForeground`·`_glyphOnFillBrush` 전부 소비처 0 확인 후 삭제(레포 전체 grep — `notes.md` 서술 제외 잔존 0). `TestPage.xaml` 아이콘 `Border`는 `CornerRadius="6"` 고정(20×20)·`Background`만 바인딩(테두리 제거), 내부 `TextBlock.Foreground`는 pill과 동일한 `StatusBrush(Status)` 재사용. pill(우측 상태 배지)은 이미 이 조합(`StatusSoftBrush`+`StatusBrush`)이라 무변경. **교훈**: 시안이 이미지로만 제공되면 렌더 결과의 시각적 "느낌"으로 재판정하다 다시 틀릴 수 있다 — 원본 HTML/JS(값이 리터럴로 박혀 있는 소스)가 있으면 그것이 최우선 근거다. **검증**: 빌드 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만, 회귀 없음). 시각 최종 확인은 사용자 몫(plan `F-8 인계 목록`의 아이콘 항목에 계속 해당). **변경 파일**: `TestPage.xaml(.cs)`.
- 2026-07-21: **작업 칸반 카테고리 헤더의 테스트 배지 — 위치 정정 + "테스트 미실행" 상태 추가** (PRD FR-T8, 시안 2장 제공). **위치**: 헤더 Grid가 `Auto|Auto|*|Auto`라 **개수 뒤 `*` 컬럼이 남는 폭을 다 먹어 배지가 열 오른쪽 끝으로 밀려 있었다** → `Auto|Auto|Auto|Auto|*`로 바꿔 배지를 카테고리·개수 바로 옆에 붙이고 마지막 `*`가 여백을 흡수하게 했다. **미실행 배지**: 기존 `BuildPassRateBadge`는 테스트가 0건이면 빈 문자열을 반환해 **배지가 아예 사라졌다**(작업만 있고 테스트를 아직 안 만든 카테고리는 배지 없음 = 상태를 알 수 없음). 이제 **테스트가 0건이거나 전부 미실행이면 "✓ 테스트 미실행" 회색 배지**를 낸다 — *실행해서 전부 실패한 0%*와 *아직 안 돌린 것*을 구분하려고 `total == 0 || executed == 0`을 기준으로 삼았다(`executed` = 상태가 미실행이 아닌 건수). 반환형을 `(string Text, bool HasResult)` 튜플로 바꾸고 `TaskColumnGroup` record에 `HasTestResult`를 추가해(호출부 1곳 + XAML 바인딩) 색을 분기 — 통과율은 초록(`#5DB463`), 미실행은 우선순위 Low와 같은 회색. 색은 **x:Bind 함수 바인딩이 ThemeResource를 못 받으므로**(함정 5) `TaskPage.xaml.cs`의 정적 브러시 + `TestBadgeForeground`/`TestBadgeBackground` 헬퍼로 반환(`PriorityBrush` 선례). 신규 resw `TaskNoTestBadge` ko/en. **"미분류" 그룹은 여전히 배지 없음**(실제 카테고리가 아니라 이름이 일치하는 테스트 카테고리가 있을 수 없다). **검증**: 빌드 오류 0·신규 경고 0. 배지 위치·색은 사용자 육안 확인 필요. **변경 파일**: `TaskPageViewModel.cs`·`TaskPage.xaml(.cs)`·`Strings/{ko-KR,en-US}/Resources.resw`.
- 2026-07-21: **테스트 화면(TestPage) 시안 정합 후속 수정 10건 — 목록 외형·필터 결함·메모 다이얼로그** (PRD FR-E1·E3·E5, plan T1~T4). 직전 재구현의 육안 확인(F-8)에서 사용자가 지적한 10건을 처리. **T1(필터 탭)**: "전체" 탭이 필터를 해제하지 못하던 결함 — 전체 탭만 `Tag=""`였고 핸들러가 `is not RadioButton { Tag: string tag }` 패턴이라, 빈 attribute가 `null`로 파싱되면 **조용히 `return`해 직전 필터("미실행")가 그대로 남는다**(체크는 되므로 밑줄·색만 바뀌어 "색상만 변한다"는 관찰과 일치). `Tag="All"` + `Tag as string`으로 바꿔 null·빈문자열·"All"을 모두 전체로 해석 — 어느 파싱 결과여도 성립. ⚠️ **가설 한계**: 빈 문자열로 파싱되고 있었다면 이 수정은 동작상 no-op이며 원인은 코드 밖이다(실동작 확인은 사용자 몫, 재현 시 런타임 재조사). 선택 탭 굵기는 `FilterTabStyle`의 `Checked`에 `Content.FontWeight=SemiBold` 추가로 해소하되, **ContentPresenter의 FontWeight가 자식에 상속**되어 개수 배지까지 굵어지므로 배지 4곳에 `FontWeight="Normal"`로 상속을 끊었다. **T2(행 재구성)**: 행 루트를 Border로 감싸 상단 1px 구분선(첫 행 선이 헤더-목록 경계가 된다), 상태 아이콘을 원형(`CornerRadius=11`) → **라운드 사각형(6)**으로 바꾸고 통과·실패는 상태색 채움+흰 글리프, 미실행은 저투명+회색 테두리로 분기(신규 헬퍼 `StatusIconBackground`/`StatusIconBorderBrush`/`StatusGlyphForeground`), 상태 pill에 `MinWidth=52`+가운데 정렬로 3종 폭 통일, 메모가 있으면 이름 아래 **인용 블록**(좌측 warning 바 + 깃발 `E7C1` + 본문, `NoteVisibility` 조건부) 표시. **hover는 `DataTemplate` 안에서 VSM `GoToState`가 동작하지 않아** `PointerEntered`/`PointerExited` 핸들러로 배경을 직접 바꿨고(색은 `UserControl.Resources`의 `RowHoverBrush`), Exited 복원값을 `null`이 아닌 `Transparent`로 둬 hit-test 영역이 좁아지지 않게 했다. **T3(스위트 헤더)**: 진행바 두께가 통과율에 따라 달라 보이던 원인은 **WinUI `ProgressBar`의 트랙과 인디케이터 높이가 서로 다른 기본 디자인** — 0%면 얇은 트랙만, 값이 있으면 두꺼운 인디케이터가 보인다. `Grid(120×4)` + 트랙/인디케이터 `Border` 2겹으로 직접 그려 두께를 확정(`IndicatorWidth(PassRate)`). 헤더 우클릭 메뉴는 **기능째 제거**(사용자 확정) — `ContextFlyout` + 핸들러 2개 + VM `RenameSuite`/`DeleteSuite`까지. `Rebuild()`의 빈 스위트 조건에서 상태 필터 종속(`&& SelectedStatus is not null`)을 빼 **표시 항목이 없는 스위트는 전체 필터에서도 카드째 숨김**. ⚠️ `TestCategory`는 작업 카테고리 이름의 **스냅샷 복사본**이라 앱 설정 변경이 전파되지 않는데 정리 UI마저 사라져, 오타·구 이름 스위트를 지울 경로가 없어졌다(Deferred `[스위트 정리 경로 부재]`). **T4(메모 다이얼로그)**: 인라인 `ContentDialog`+맨 TextBox → 신규 `TestNoteDialog`(ContentDialog 서브클래스, 제목 "메모 추가" + 대상 테스트명 부제 + 안내 placeholder + 취소/저장). placeholder는 **접미 없는 베어네임 resw라 `x:Uid`로 붙이면 빌드는 통과하되 빈 문자열**이 되므로(함정 3) 코드비하인드에서 `PlaceholderText`로 주입. 신규 키 `TestNote_Title`·`TestNote_Placeholder` ko/en 양쪽. **아이콘 재수정(같은 날 재지적)**: 렌더를 시안과 다시 대조한 결과 **미실행이 "라운드 사각형 테두리 + 내부 ○ 글리프"라 원이 이중으로 보여** 시안(속이 빈 원 하나)과 크게 달랐다. Q1 답변("세 상태 모두 라운드 사각형")보다 **시안을 우선**해 정정 — 미실행만 `CornerRadius=11`(원)·배경 투명·글리프 없음으로, 통과·실패는 라운드 사각형 채움 유지(`StatusIconCornerRadius` 추가, `StatusGlyph`의 미실행은 빈 문자열, `StatusIconBackground`의 미실행은 투명). **교훈**: 아이콘 형태처럼 렌더로만 판정되는 항목은 텍스트 선택지(preview)로 합의해도 실제 화면과 어긋날 수 있어, 시안 이미지가 있으면 그것이 최종 기준이다. **Phase F 반영**: 완료 리뷰가 ① FR-E5의 "**에러** 표시" 몫이 미착수인데 PRD Coverage가 ✅로 오표기됨(`TestItem`에 에러 필드 자체가 없음) → 부분 커버로 정정하고 대장 `[FR-E5 에러 표시 미착수]` 등재, ② **`Border.Background`는 테두리 안쪽에만 그려지므로** 채움 아이콘에 `BorderThickness=1.5`를 남기면 색 사각형이 19×19가 돼 미실행(22×22)보다 작아 보임 → `StatusIconBorderThickness`로 채움 상태는 0을 반환하게 수정. **검증**: 클린 리빌드 오류 0·경고 5건(기존 NU1903 1 + CS0612 4)만, task별 spec·quality 리뷰 지적 0(T3 quality의 고아 resw MAJOR 1건은 plan D11·대장의 계획된 이연임을 근거로 반증해 철회). **시각·실동작은 사용자 육안 확인 대기**(plan `## F-8 인계 목록`). **변경 파일**: `TestPage.xaml(.cs)`·`TestPageViewModel.cs`·`Resources/Styles.xaml`·`Views/Dialogs/TestNoteDialog.xaml(.cs)`(신규)·`Strings/{ko-KR,en-US}/Resources.resw`.
- 2026-07-21: **테스트 화면(TestPage)·테스트 등록 다이얼로그 시안 정합 재구현 — 스위트를 "작업 카테고리"로 정렬해 칸반 통과율 배지 배선 완성** (PRD `docs/prd.md` FR-E1·E3·E5 + FR-E4/FR-T8, plan T1~T5). 사용자가 "테스트 화면부터 잘못 구현돼 배지가 안 뜬다"며 시안 이미지 2장 제공(① 새 테스트 등록 다이얼로그 ② 테스트 화면). **배경**: 배지는 "작업 카테고리 이름 == 테스트 스위트 이름"일 때만 뜨는데(FR-T8), 테스트 스위트는 자유 입력이라 이름이 어긋나 사실상 죽어 있었다 → 이번에 **스위트 선택지를 작업 카테고리로 고정**해 근본 해소. **T1**: `TestPageViewModel` 생성자에 `AppSettings` 주입(호출부 `ProjectCardViewModel.CreateTestPageViewModel` 1곳), `AvailableCategories`(작업 카테고리)·`SelectedSuiteFilter` 추가, `Rebuild()`에 스위트 필터를 상태 필터와 **직교** 적용(통계 카드·탭 개수는 프로젝트 전체 기준 유지). 품질 리뷰 MAJOR로 카테고리 결합식이 3곳(TaskEditDialogViewModel·TaskPageViewModel·신규) 중복임이 드러나 `AppSettingsDialogViewModel.ResolveTaskCategories(AppSettings)`로 추출(3회 문턱), 쓰기 전용 `_settings` 필드 제거. **T2**: 등록/편집 다이얼로그 스위트를 자유 입력 ComboBox → **작업 카테고리 비편집 드롭다운**(`SelectedItem` 바인딩), 라벨 "스위트 (작업 카테고리)", 이름 필수 `*`+하단 danger 라인(TaskEditDialog 선례), 라벨 `InputLabelStyle`, 제목 "새 테스트 등록"·새 테스트 버튼 "등록"(신규 키 `TestEdit_Submit`), placeholder 시안 문구. **중요 방어**: 편집 대상의 기존 스위트가 작업 카테고리에 없으면(과거 자유명·"작업" 스위트) VM 생성자가 그 값을 옵션에 보강 — 안 하면 ComboBox 선택이 비어 필수 검증에 막히고 **기존 스위트가 소실**된다. **T3**: 헤더에 프로젝트명 배지(`TagBadgeStyle`+`AppMutedSoftBrush`)·스위트 필터 드롭다운("전체"+작업 카테고리)·"+ 테스트 등록" 버튼. **T4**: 통계 카드를 가로 3등분(`StatCardStyle` 신규, 좌 색점+라벨 / 우 22pt 큰 숫자)으로, 상태 필터를 헤더 라디오 → **개수 배지 포함 밑줄형 탭바**(`FilterTabStyle` 신규)로 이설. `FilterTabStyle`은 **함정 7**대로 선택 밑줄(CheckStates)과 호버 오버레이(CommonStates)를 별도 요소로 분리. **T5**: 테스트 항목을 카드 → **행**(좌 원형 상태 아이콘 22px + 이름 / 우 상태 pill), 스위트 그룹을 카드형 Border + 헤더(폴더 아이콘·이름·"N/M 통과"·우측 진행바 120px)로 재구성. 인라인 콤보·버튼을 없애고 **상태 pill 클릭 순환(통과→실패→미실행) + 우클릭 메뉴**(행: 수정/메모/삭제, 헤더: 이름수정/삭제)로 대체 — 발견성 보완용 pill 툴팁 추가. 신규 정적 헬퍼 `StatusSoftBrush`(0x28 알파)·`StatusText`·`FormatPassCount`·`NextStatus`(함정 5: x:Bind 함수 바인딩은 Brush 직접 반환). 마지막 소비처가 사라진 `StatusOptions`·`TestStatusOption`·`NoteVisibility`·`MethodVisibility`·`FormatPassRate`·`StatusCombo_*` 제거(잔존 참조 0, TaskPage 동명 심볼은 별개라 무영향). **변경 파일**: `TestPageViewModel.cs`·`ProjectCardViewModel.cs`·`AppSettingsDialogViewModel.cs`·`TaskPageViewModel.cs`·`TaskEditDialogViewModel.cs`·`TestEditDialog.xaml(.cs)`·`TestEditDialogViewModel.cs`·`TestPage.xaml(.cs)`·`Resources/Styles.xaml`·`Strings/{ko-KR,en-US}/Resources.resw`. resw는 ko/en 대칭 유지(함정 2), x:Uid 소비 키는 접미 형식 유지(함정 3). 도메인·스키마·직렬화 무변경. **검증**: MSBuild x64 클린 리빌드 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만), 전 task spec·quality 리뷰 통과(T1 MAJOR 1+MINOR 1 수정 후 재리뷰 OK, 나머지 지적 0). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 다이얼로그 스위트 드롭다운·이름 하단 라인 / 헤더 배지·필터 / 통계 카드·탭 밑줄(선택 탭 호버 시 밑줄 유지 = 함정 7 회귀) / 스위트 그룹·행 레이아웃·pill 클릭 순환·우클릭 메뉴 / **통합: 새 테스트를 작업 카테고리 스위트로 등록 후 칸반 그 카테고리 그룹에 통과율 배지가 뜨는지**(궁극 목표). **미결 follow-up(Deferred)**: NotificationPage 시안 대조 / 이미지의 실패색이 붉으나 PRD·코드는 호박(#E8B45A) 유지 / 기존 자유명·"작업" 스위트 테스트는 마이그레이션 안 함(go-forward) / 상태 soft 브러시의 Palette 이관 검토 / 행에서 방법·메모 미표시(데이터·편집 경로는 유지) / 스위트 조작이 우클릭이라 발견성 낮을 수 있음. 브랜치 `task/testpage-design-align` — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **작업 칸반 카테고리 그룹 통과율 배지가 안 뜨던 버그 수정 (FR-T8 배선 완성)**. 사용자가 이미지처럼 칸반 카테고리 헤더 옆 통과율 배지("✓ 통과율 67% · 4건")가 안 보인다고 신고. **조사 결과 배지 계산·렌더 코드는 이미 정상**: `TaskPageViewModel.BuildPassRateBadge`(:153-166)가 `AppSuccessSoftBrush` pill로 렌더(`TaskColumnGroupTemplate`, `TaskPage.xaml:135-146`), 데이터도 `CreateTaskPageViewModel`이 `EnsureTestsLoaded()`로 로드함(로딩 문제 아님). **근본 원인**: `BuildPassRateBadge`는 FR-T8 명세대로 "작업 카테고리 이름 == 테스트 스위트 이름"일 때만 배지를 표시하는데, "테스트 추가" 토글로 만든 링크 테스트는 `CreateLinkedTest`가 **항상 고정 "작업"(TaskLinkedTestCategory) 스위트**로 넣어 이름이 어긋남 → 사용자가 테스트 페이지에서 작업 카테고리와 같은 이름의 스위트를 손수 만들지 않는 한 배지가 영구히 미표시(이미지의 "67%·4건"은 4건으로 67%가 불가능한 조합이라 시안 placeholder — 실제 렌더된 적 없음). **수정(사용자 승인 방향: 링크 테스트를 카테고리별 스위트로)**: `CreateLinkedTest`(`TaskPageViewModel.cs:206`)가 링크 테스트를 `todo.Category` 이름의 스위트(없으면 생성, 대소문자 무시)에 넣도록 변경, 빈 카테고리(미분류)는 배지 비대상이라 기존 "작업" 스위트로 폴백. 변수 `workCategory`→`suite`, docstring·주석 갱신. 단일 파일·약 6줄. 호출부(`TaskPage.xaml.cs:193`)는 `AddTodo`로 `todo.Category` 설정 후 호출되어 안전. `TaskLinkedTestCategory` resw는 폴백에 계속 쓰여 존치. PRD FR-T6에 배선 정정 노트 추가. **go-forward만**: 기존에 "작업" 스위트로 들어간 링크 테스트는 이동하지 않으므로 과거 데이터는 배지에 반영 안 됨(마이그레이션 미실시 — 위험·범위 초과). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만), code-quality 리뷰 BLOCKER/MAJOR/MINOR 0. **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 새 작업을 카테고리(예: UI·UX) 지정 + "테스트 추가" ON으로 만든 뒤, 칸반 그 카테고리 그룹 헤더에 통과율 배지가 뜨는지 / 테스트 페이지에 그 카테고리 이름 스위트가 생겼는지.
- 2026-07-21: **작업(TaskPage) 카드 — 제목·설명 최대 2줄 표시 + 말줄임(...)** (2회 수정). 사용자가 칸반 카드에서 제목이 1줄로만 보이고 긴 설명이 잘림 없이 전부 표시되던 것을, 제목 2줄·설명 2줄로 제한하고 넘치는 부분은 `...`로 줄여 보이게 요청. **1차 수정(불충분)**: `TaskPage.xaml` 카드 템플릿의 `TextBlock`에 `MaxLines="2"` + `TextTrimming="CharacterEllipsis"` 추가(칸반 제목·설명, 목록 `TaskCardTemplate` 제목). **1차만으로는 무효** — 사용자 재확인 결과 카드가 가로로만 늘어나고 여전히 1줄. **근본 원인**: 칸반 뷰 `ScrollViewer`가 `HorizontalScrollMode="Auto"`라 콘텐츠(내부 `Grid`)에 **무한 너비**를 전달 → `Width="*"` 4개 열이 뷰포트에 맞춰지지 않고 카드 텍스트 길이만큼 무한정 늘어남 → 너비 제약이 없으니 `TextWrapping`/`MaxLines`가 애초에 발동하지 않음. **2차 수정(핵심)**: 내부 `Grid`에 `Width="{Binding ViewportWidth, ElementName=KanbanScrollViewer}"`(뷰포트 너비에 묶어 유한화 → 열 균등 분배·텍스트 줄바꿈 동작) + `MinWidth="950"`(열 4×230 + 간격 3×10 = 950; 좁은 창에선 가로 스크롤 유지) 부여, `ScrollViewer`에 `x:Name="KanbanScrollViewer"` 지정. 목록 뷰는 이미 `MaxWidth="520"`으로 너비가 유한해 1차 제목 수정만으로 정상. 로직·바인딩(데이터)·resw·스키마 무변경(마크업만). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 긴 제목·설명 카드가 열 너비 안에서 각각 2줄까지 표시되고 초과분이 `...`로 잘리는지 / 창을 좁혔을 때 가로 스크롤이 정상 동작하는지.
- 2026-07-21: **작업 편집 다이얼로그 — 미분류(빈 카테고리) 항목 편집 시 콤보가 비어 보이던 문제 수정**. 사용자가 미분류 작업을 클릭해 편집하면 카테고리 콤보가 빈 상태로 열리던 것을 첫 항목이 표시되게 요청. **원인**: 직전 작업(T2, D6)에서 편집 모드의 빈 카테고리를 `null`(미선택)로 열어 그대로 저장 시 빈 값을 보존하도록 의도했으나, 사용자는 콤보가 비어 보이는 것을 원치 않음. **수정**: `TaskEditDialogViewModel.cs:80`의 편집 로드 분기를 `string.IsNullOrEmpty(existing.Category) ? null : existing.Category` → `? CategoryOptions[0] : existing.Category`로 변경(첫 카테고리 "UI·UX" 기본 표시), 주석도 갱신. `BuildResult`의 `?? string.Empty` 방어는 그대로 유지(무영향). **의도된 동작 변경**: D6 결정을 뒤집는 것으로, 미분류 항목을 편집 후 저장하면 카테고리가 "UI·UX"로 확정되는 것이 요청된 정상 동작이다. 마크업·resw·스키마 무변경(1줄+주석). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **새 작업 다이얼로그 — "테스트 추가" 카드 토글이 세로로 내려가던 문제 수정(시안 정합)**. 사용자가 현재 화면(토글이 "테스트 추가" 텍스트 **아래 왼쪽**에 세로 배치)과 시안(토글이 헤더 **오른쪽**에 인라인)을 비교하며 시안대로 수정 요청. **원인**: "테스트 추가"는 `toolkit:SettingsCard`(CommunityToolkit.WinUI.Controls.SettingsControls 8.2)로 구현돼 있는데, `SettingsCard`는 카드 폭이 `SettingsCardWrapThreshold`(기본 476px)보다 좁으면 콘텐츠(토글)를 헤더 아래로 내리는 적응형 세로 wrap이 동작한다. 다이얼로그 콘텐츠 `MinWidth="360"`이라 카드 실폭이 476px 미만 → wrap 발동 → 토글이 아래로 내려감(직전 07-21 항목에서 "이미 시안과 동일, 무변경"으로 판단했으나 실제 앱은 wrap 상태였음 — 정정). **수정**: `TaskEditDialog.xaml`의 해당 `SettingsCard.Resources`에 `SettingsCardWrapThreshold`·`SettingsCardWrapNoIconThreshold`를 각 **0**으로 덮어써 세로 wrap을 끔(카드가 항상 NormalState=토글 우측 인라인 유지). CommunityToolkit에서 wrap을 끄는 표준 방식(임계값 리소스 오버라이드). 토글 색(ON=살몬/OFF=회색)은 `IsOn`(`Vm.AddTest`) 상태에 따른 accent 색으로 기존과 동일 — 시안2의 빨간 토글은 ON 상태일 뿐이며 별도 변경 아님. 마크업만 변경(로직·바인딩·resw 무변경, 5줄 추가). 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 다이얼로그를 열었을 때 "테스트 추가" 토글이 헤더 오른쪽에 인라인으로 정렬되는지. 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).
- 2026-07-21: **새 작업 다이얼로그 — 등록 버튼 필수 입력 기반 활성/비활성화** (시안 피드백 후속). 사용자가 "테스트 추가 항목을 이미지와 동일하게 + 등록 버튼은 필수 항목이 모두 입력되면 활성, 미입력이면 비활성" 요청. **① "테스트 추가" 항목은 이미 시안과 동일** — `toolkit:SettingsCard`(Header "테스트 추가" + Description "이 작업을 테스트 목록에도 추가합니다." + 우측 `ToggleSwitch`)로 직전 작업에서 구현·커밋된 상태라 무변경(검토만). **② 등록/저장 버튼 활성화**: `ContentDialog`의 primary 버튼을 필수 입력(제목) 충족 시에만 활성화. `TaskEditDialogViewModel`에 계산 프로퍼티 `CanSubmit => !string.IsNullOrWhiteSpace(Title)` 추가, `Title`에 `[NotifyPropertyChangedFor(nameof(CanSubmit))]` 부여(제목 입력 시 실시간 갱신). `TaskEditDialog.xaml` `ContentDialog` 루트에 `IsPrimaryButtonEnabled="{x:Bind Vm.CanSubmit, Mode=OneWay}"` 바인딩(`ContentDialog`는 `FrameworkElement`라 기존 헤더 x:Bind처럼 정상 동작). 새 작업은 제목이 비어 열리므로 등록 버튼이 **초기 비활성**, 제목 입력 시 활성. 편집 모드는 제목이 미리 채워져 활성 상태로 열림. 기존 `OnSave`의 `args.Cancel` 빈 제목 차단은 방어선으로 유지. 빌드 MSBuild x64 오류 0·신규 경고 0(기존 NU1903 1 + CS0612 4만). **빌드로 검증 불가 — 사용자 확인 필요(⏳ HUMAN-VERIFY)**: 새 작업 열었을 때 등록 버튼 회색(비활성) → 제목 입력 시 활성 전환. 브랜치 `task/taskedit-dialog-design-align`(이어서) — master 병합 미실행(사용자 승인 대기).

## 미해결 이슈
- (없음)

## 주의사항
- System.Drawing.Common NuGet 패키지 추가됨 (아이콘 추출용)
- LauncherItems 테이블이 SQLite에 추가됨 (기존 DB 자동 마이그레이션)
- MainWindow 생성자에 LauncherRepository 파라미터 추가됨
- 런처 아이템은 Button 대신 Grid 사용 (CanDrag 호환성 — Button은 포인터 캡처로 드래그 불가)
- 런처 드래그&드롭: DashboardView와 동일한 DropPlaceholder 패턴 사용 (DisplayItems: ObservableCollection&lt;object&gt;)

## 아카이브 인덱스
- 2026-03.md: 동기 DB 호출 백그라운드 전환, Import 개선, 앱 안정성(전역 예외 처리)
- 2026-04.md: 런처 중복 클릭 방지·프리징 개선, 태그 마키 최적화, README·LICENSE 신규 작성
- 2026-05.md: 커맨드 스크립트 PowerShell 따옴표 손실 수정(EncodedCommand 전환)
- 2026-06.md: 테스트 목록 기능 최초 구현
- 2026-07.md: 리디자인 Phase 0~5(다크 팔레트·설정·칸반·테스트 전체 페이지·작업기록·알림), TaskEditDialog 시안 정합, To-Do/테스트 상태 모델 ComboBox 전환, 테스트 카테고리 구조
